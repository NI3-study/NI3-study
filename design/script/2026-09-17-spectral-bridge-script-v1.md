# 스펙트럼 다리: 뺄셈 하나가 연산자가 되기까지

> **이 문서가 처음으로 하는 것** — 간선 하나 위의 **뺄셈**에서 출발해 [03 §2.3](03_bridge_mlp_to_gcn.md#build-operator)에서 만든 그 $\hat A$까지를 열다섯 걸음으로 자라게 한다. 논문이 최종식 앞에 스펙트럼 이야기를 두 쪽 적어 둔 이유가 여기 있고, 그 두 쪽을 논문의 순서대로 다시 읽는 곳이 다음 문서인 [02 논문 해설](02_kipf2017_gcn_guide.md)이다. 읽는 순서는 이 문서가 세 번째다.

03을 읽고 나면 $\hat A$가 **무엇인지**는 안다. $\tilde A=A+I$를 만들고, 차수를 다시 세고, 양쪽을 제곱근으로 나눴다. 그런데 논문은 그 식을 그렇게 소개하지 않는다. 논문은 식 (2)를 먼저 적고 "이 전파 규칙의 형태는 그래프 위 국소 스펙트럼 필터의 1차 근사로 **동기를 줄 수 있다**"고 말한 뒤, 두 쪽에 걸쳐 전혀 다른 곳에서 출발한다. 이 문서의 질문은 하나다 — **GCN 한 층의 정점 축 연산 $\hat A$는 어디서 왔는가.**

들고 들어가는 것은 넷뿐이다. G4와 신호 $x=(1,2,3,4)$, 행렬 곱, 그리고 "$Av=\lambda v$"라는 문장 하나. 들고 나가는 것은 [종점](#two-sentences)의 두 문장이다. 그 사이의 열다섯 걸음은 각각 **직전 걸음이 남긴 불편 하나**를 첫 문장으로 꺼내고, 딱 하나만 바꾼다. 그러니 각 단계에서 "그래서 다음이 왜 필요한가"를 스스로 말할 수 있으면 제대로 읽은 것이다.

## 학습 목표 { #goals }

- 간선 양 끝 값의 차이 하나가 어떻게 행렬 $\Delta$, $L$, 그리고 $\hat A$로 자라는지 순서대로 말한다.
- G4에서 $Lu=\lambda u$를 만족하는 네 신호를 보고 한 행 대입으로 검산한다.
- 고유값 $\lambda$가 "그 방향의 거칢"이라는 것을 정수 네 개로 확인한다.
- 신호 하나를 네 방향으로 쪼개고, 모드마다 수를 곱하고, 되돌리는 세 단계를 스펙트럼 합성곱의 정의로 말한다.
- 파라미터 개수가 $4\to K{+}1\to2\to1\to C\times F$로 줄어드는 사슬에서 각 감소가 무엇을 대가로 치렀는지 말한다.
- renormalization trick이 무엇을 고쳤고 무엇을 내주었는지 G4 숫자로 설명한다.

**이 문서의 숫자 규약.** 그래프는 계속 G4($E=\{(1,2),(1,3),(2,4)\}$, 경로 $3\!-\!1\!-\!2\!-\!4$)이고 신호는 계속 $x=(1,2,3,4)$다. 차수는 **두 종류**가 나오고 매번 어느 쪽인지 적는다 — 자기 연결이 **없는** $d=(2,2,1,1)$은 S4부터 S13까지, 자기 연결을 넣고 다시 센 $\tilde d=(3,3,2,2)$는 S14부터다. 고유벡터의 부호는 첫 비영 성분을 양수로 맞춘다(반대 부호를 써도 같은 방향이지만 분해 계수의 부호가 통째로 뒤집힌다). 태그 규약은 [index](index.md#자료를-읽는-방법)와 같다.

---

## S1. 간선 하나를 건널 때 두 값이 얼마나 다른가 { #s1 }

먼저 계산부터 한다. 새 용어는 없다.

G4의 간선은 셋이다. 각 간선에서 **양 끝 값의 차**를 적는다.

$$
x_1-x_2=1-2=-1,
\qquad
x_1-x_3=1-3=-2,
\qquad
x_2-x_4=2-4=-2.
$$

정수 뺄셈 세 번이 전부다. 제곱근도, 행렬도, 차수도 아직 없다.

이 세 숫자가 이 문서의 씨앗이다. 그리고 이것은 새로 지어낸 것이 아니다 — 논문의 **첫 번째 식**에 이미 인쇄되어 있다. 아직 식 번호를 회수하지는 않는다. 숫자 셋을 숫자 하나로 만들어야 한다.

## S2. 신호 하나에 숫자 하나 — 거칢 { #s2 }

간선마다 숫자가 따로 나오면 신호끼리 비교할 수 없다. 신호 하나에 숫자 하나를 붙이자.

부호를 없애려고 제곱하고, 다 더한다.

$$
E(x)=(x_1-x_2)^2+(x_1-x_3)^2+(x_2-x_4)^2
=1+4+4=9.
$$

이 숫자에 이름을 붙이자: **거칢**이다. 신호가 간선을 건널 때 얼마나 튀는지를 재는 하나의 점수다. 값이 전부 같으면, 예를 들어 $x=(1,1,1,1)$이면 세 차가 모두 0이므로 거칢은 정확히 0이다. 크기만 키운 $x=(2,4,6,8)$은 차가 두 배가 되므로 거칢이 네 배인 36이다 — **거칢은 신호의 크기에도 함께 반응한다.** 이 사실은 S6에서 다시 쓴다.

**논문 식 회수.** 방금 만든 것이 논문의 식 (1) 안에 있는 $\mathcal L_{reg}$다.

[원문] 논문은 라벨 정보를 그래프 위에서 매끄럽게 퍼뜨리는 기존 방식의 예로 손실에 정규화 항을 더하는 형태를 든다. $\mathcal L=\mathcal L_0+\lambda\mathcal L_{reg}$이고 $\mathcal L_{reg}=\sum_{i,j}A_{ij}\lVert f(X_i)-f(X_j)\rVert^2=f(X)^\top\Delta f(X)$이며, "식 (1)의 형태는 그래프에서 연결된 정점이 같은 라벨을 가질 가능성이 높다는 가정에 의존한다"고 적는다. [Kipf & Welling 2017, §1, Eq. (1)]

!!! note "논문의 좌변은 9가 아니라 18이다"
    식 (1)의 $\sum_{i,j}$는 순서쌍에 대한 이중합이다. 무방향 그래프에서는 간선 하나가 $(i,j)$와 $(j,i)$로 두 번 세어지므로 G4에서 좌변은 $2\times9=18$이 된다. 우변 $f^\top\Delta f$도 같은 규약이면 18이어야 하는데, $\Delta=D-A$로 계산한 값은 9다(S3). 상수 2의 차이이므로 논문의 논지에는 영향이 없지만, **숫자를 맞춰 보는 독자는 여기서 처음 걸린다.** 이 문서는 간선당 한 번 세는 쪽, 즉 9를 쓴다.

파라미터는 아직 0개다. 여기까지는 학습이 아니라 채점이다.

## S3. 같은 9를 행렬 한 번으로 — 그래프 라플라시안 { #s3 }

간선을 하나씩 훑어 더하는 것은 행렬 곱이 아니다. forward 계산 안에 넣으려면 $x$에 곱해지는 **행렬**이 있어야 한다.

차수 행렬 $D=\operatorname{diag}(d_1,\dots,d_N)$을 두고 $\Delta=D-A$를 만든다. G4에서 $d=(2,2,1,1)$이므로

$$
\Delta=
\begin{bmatrix}
2&-1&-1&0\\
-1&2&0&-1\\
-1&0&1&0\\
0&-1&0&1
\end{bmatrix},
\qquad
\Delta x=
\begin{bmatrix}-3\\-1\\2\\2\end{bmatrix}.
$$

[해설: 도출] 한 행만 손으로 확인하면 나머지는 같은 산술이다. 1행은 $2(1)-(2+3)=-3$이다. 일반형으로는

$$
(\Delta x)_i=d_ix_i-\sum_{j\in\mathcal N(i)}x_j=d_i\left(x_i-\overline{x}_{\mathcal N(i)}\right)
$$

이므로 **$\Delta$의 한 행은 "자기 값에서 이웃 평균을 뺀 것에 차수를 곱한 값"** 이다. [01 §6](01_gnn_gentle_guide.md#aggregation)의 평균 집계가 여기서 다시 나온다.

이제 거칢을 행렬로 잰다.

$$
x^\top\Delta x=1(-3)+2(-1)+3(2)+4(2)=-3-2+6+8=9.
$$

S2의 9와 **같은 9**다. 간선을 훑는 절차가 행렬 하나로 접혔다.

이 행렬에 이름을 붙이자: **그래프 라플라시안** $\Delta=D-A$다(정규화하지 않았다는 뜻으로 비정규화 라플라시안이라고도 한다).

**논문 식 회수.** 방금 만든 것이 식 (1) 우변의 $\Delta$다. 파라미터는 여전히 0개다.

## S4. 차수로 눌러 준다 — 정규화 라플라시안 { #s4 }

$\Delta$는 차수가 큰 정점의 행을 그냥 키운다. 1행의 계수가 $(2,-1,-1,0)$이고 3행은 $(-1,0,1,0)$이다 — 같은 규칙인데 크기가 다르다. [03 §2.3](03_bridge_mlp_to_gcn.md#build-operator)에서 이미 한 번 고친 문제다. 같은 처방을 쓴다.

간선 $(i,j)$의 계수를 양 끝 차수의 제곱근으로 한 번씩 나눈다.

$$
L=D^{-1/2}\Delta D^{-1/2}=I_N-D^{-1/2}AD^{-1/2}.
$$

**여기서 쓰는 차수는 $d=(2,2,1,1)$이다.** 자기 연결을 더한 뒤의 $\tilde d=(3,3,2,2)$가 아니다. 03이 "$D$가 아니라 $\tilde D$인 이유는 하나"라고 못 박은 바로 그 자리인데, 이번에는 정규화할 대상이 $\tilde A$가 아니라 원래 그래프 $A$이므로 차수도 원래 것을 쓴다. 두 차수가 다시 만나는 곳은 S14다.

$D^{-1/2}AD^{-1/2}$가 앞으로 자주 나오므로 $S$로 줄여 쓴다. **새 개념이 아니라 줄임 표기다.** G4에서

$$
S=D^{-1/2}AD^{-1/2}=
\begin{bmatrix}
0&\tfrac12&\tfrac1{\sqrt2}&0\\
\tfrac12&0&0&\tfrac1{\sqrt2}\\
\tfrac1{\sqrt2}&0&0&0\\
0&\tfrac1{\sqrt2}&0&0
\end{bmatrix},
\qquad
L=I-S.
$$

$S_{12}=1/\sqrt{2\cdot2}=0.5$이고 $S_{13}=S_{24}=1/\sqrt{2\cdot1}=0.7071$이다. 따라서 $L$의 대각은 전부 1, $L_{12}=-0.5$, $L_{13}=L_{24}=-0.7071$이다.

거칢을 다시 잰다.

$$
Lx=(-2.1213,\,-1.3284,\,2.2929,\,2.5858),
\qquad
x^\top Lx=12.4437.
$$

[해설: 도출] 이 값도 간선 셋으로 쪼개진다. $x^\top Lx=\sum_{(i,j)\in E}\left(\dfrac{x_i}{\sqrt{d_i}}-\dfrac{x_j}{\sqrt{d_j}}\right)^2$이고, 간선 $(1,2)$는 $(1/\sqrt2-2/\sqrt2)^2=0.5$, 간선 $(1,3)$은 $(0.7071-3)^2=5.2574$, 간선 $(2,4)$는 $(1.4142-4)^2=6.6863$이다. 합이 12.4437이다. **S2의 뺄셈이 그대로 남아 있고, 양 끝을 각자의 차수로 나눈 것만 달라졌다.**

이 행렬에 이름을 붙이자: **정규화 라플라시안** $L$이다.

**논문 식 회수.** 논문이 §2.1에서 쓰는 라플라시안이 바로 이것이다.

[원문] "$U$는 정규화 그래프 라플라시안 $L=I_N-D^{-1/2}AD^{-1/2}=U\Lambda U^\top$의 고유벡터 행렬이다." [Kipf & Welling 2017, §2.1]

파라미터는 여전히 0개다. $U$와 $\Lambda$가 무엇인지는 아직 모른다 — 다음 두 걸음이 그것이다.

## S5. 통과해도 모양이 안 변하는 신호 — 고유벡터와 고유값 { #s5 }

12.4437은 $x$ 하나에 대한 값이다. 다른 신호를 재려면 처음부터 다시 계산해야 한다. **눈금자**가 필요하다. 신호마다 계산하지 않고 "이 신호는 저 신호보다 얼마나 거친가"를 바로 말할 수 있는 기준 신호 몇 개.

기준이 될 만한 신호를 이렇게 고른다 — **$L$을 통과해도 성분 비율이 그대로이고 크기만 변하는 신호.**

$$
Lu=\lambda u.
$$

$L$은 대칭이고 $N=4$이므로 이런 방향이 정확히 넷 있다(서로 수직이 되도록 고를 수 있다). G4의 넷은 다음과 같다.

| | $u$ (성분 순서는 $v_1,v_2,v_3,v_4$) | $\lambda$ |
|---|---|---:|
| $u_1$ | $(0.5774,\ 0.5774,\ 0.4082,\ 0.4082)$ | $0$ |
| $u_2$ | $(0.4082,\ -0.4082,\ 0.5774,\ -0.5774)$ | $0.5$ |
| $u_3$ | $(0.4082,\ 0.4082,\ -0.5774,\ -0.5774)$ | $1.5$ |
| $u_4$ | $(0.5774,\ -0.5774,\ -0.4082,\ 0.4082)$ | $2$ |

찾는 방법은 이 문서의 주제가 아니다. **주어진 것을 확인하는 데는 한 행이면 된다.**

[해설: 도출] $L$의 3행은 $(-0.7071,\ 0,\ 1,\ 0)$이다. 이것을 $u_1$과 곱하면

$$
-0.7071(0.5774)+1(0.4082)=-0.4082+0.4082=0
$$

이고, 우변은 $\lambda\,(u_1)_3=0\times0.4082=0$이다. 맞는다. 나머지 셋도 한 행이면 끝난다.

이 넷에 이름을 붙이자: 방향 $u_k$를 **고유벡터**, 배수 $\lambda_k$를 **고유값**이라 한다. $Av=\lambda v$라는 문장을 이미 본 적이 있다면 같은 문장이다. 달라진 것은 $v$가 이제 **정점 위의 숫자 한 벌**, 즉 그래프 위의 신호라는 것뿐이다.

**부호 규약.** $u$가 해라면 $-u$도 해다. 이 사이트는 첫 비영 성분을 양수로 맞춘다. 반대 규약을 쓰면 S7의 분해 계수 부호가 통째로 뒤집히므로, 다른 자료의 숫자와 부호가 어긋나면 먼저 이것부터 확인한다.

아래 그림에서 **원반의 $+$/$-$ 패턴이 간선을 따라 몇 번 바뀌는지** 세어 보면, 그 횟수가 표의 $\lambda$ 순서와 정확히 같다.

<div class="gnn-fig-slot" data-gnn-fig="spec-eigen" markdown="0">
<p class="gnn-fallback">정지 프레임 5장. ① 우리 신호 x = (1, 2, 3, 4)를 L에 통과시킨 결과 Lx = (−2.121, −1.328, 2.293, 2.586)을 x 옆에 나란히 놓은 것. 두 격자의 행 비율이 서로 달라서 x는 이 조건을 만족하지 않는다 — x는 고유벡터가 아니다. ②~⑤ 조건을 만족하는 네 신호. ② u₁ = (0.577, 0.577, 0.408, 0.408), λ = 0, 부호 변화 0회. ③ u₂ = (0.408, −0.408, 0.577, −0.577), λ = 0.50, 부호 변화 1회. ④ u₃ = (0.408, 0.408, −0.577, −0.577), λ = 1.50, 부호 변화 2회. ⑤ u₄ = (0.577, −0.577, −0.408, 0.408), λ = 2.00, 부호 변화 3회. 각 프레임에서 부호가 바뀌는 간선은 점선으로 그리고, 격자 옆에 검산 한 줄 — L의 한 행과 그 열을 곱한 값이 λ배와 같다 — 을 숫자로 적는다.</p>
</div>

첫 프레임이 중요하다. $x=(1,2,3,4)$는 이 조건을 만족하지 **않는다.** $Lx$의 행 비율이 $x$의 행 비율과 다르기 때문이다. 고유벡터는 특별한 신호이고, 우리 신호는 그중 하나가 아니다. 그 간극을 메우는 것이 S7이다.

**논문 식 회수.** 방금 만든 넷을 열로 세운 행렬이 논문의 $U$이고, 네 배수를 대각에 넣은 것이 $\Lambda$다. 그래서 $L=U\Lambda U^\top$이다. 파라미터는 여전히 0개다.

## S6. 고유값이 재는 것 — 모드와 스펙트럼 { #s6 }

넷을 찾았지만 $\lambda=0,\,0.5,\,1.5,\,2$가 **무엇을 재는 눈금인지**는 아직 모른다. 재 보면 된다.

소수점을 없애기 위해 같은 방향을 정수로 다시 쓰고(방향만 같으면 되므로 상수배는 자유다), 각각에 대해 S4의 거칢 $v^\top Lv$를 잰다.

| 방향(정수판) | $v^\top Lv$ — S4의 거칢 | $\lVert v\rVert^2$ | 몫 |
|---|---:|---:|---:|
| $(\sqrt2,\ \sqrt2,\ 1,\ 1)$ | $0$ | $6$ | $0$ |
| $(1,\ -1,\ \sqrt2,\ -\sqrt2)$ | $3$ | $6$ | $0.5$ |
| $(1,\ 1,\ -\sqrt2,\ -\sqrt2)$ | $9$ | $6$ | $1.5$ |
| $(\sqrt2,\ -\sqrt2,\ -1,\ 1)$ | $12$ | $6$ | $2$ |

정수 넷이 깔끔하게 나오는 것은 G4가 경로 그래프이기 때문이다. 그리고 마지막 열이 $\lambda$다. 그러니까 **$\lambda$는 그 방향의 거칢을 크기로 나눈 값**이다. S2에서 거칢이 신호 크기에 함께 반응한다고 적었는데, 크기로 나누면 그 반응이 사라져 순수한 "모양의 거칢"만 남는다.

여기서 재는 거칢은 S4의 것, 곧 **차수로 한 번씩 나눈 판**에서 잰 값이다. S2의 날것 $E(x)$가 아니다. 두 판을 섞으면 숫자가 맞지 않는다.

간선을 따라 부호가 바뀌는 횟수도 순서대로 $0,\,1,\,2,\,3$이다. $\lambda$가 큰 방향일수록 이웃과 더 자주 어긋난다.

두 이름을 붙이자. 고유벡터 하나하나를 **모드**라 부르고, 고유값 전부를 모은 집합 $\operatorname{spec}(L)=\{0,\,0.5,\,1.5,\,2\}$를 **스펙트럼**이라 부른다. 이 문서의 제목에 있는 그 말이다. **이 사이트의 한국어 본문은 이 개념을 언제나 "스펙트럼"이라고만 쓴다** — 논문이 쓰는 형용사형이 어떤 낱말인지는 S8에서 한 번 괄호로 보여 준다.

!!! warning "$\lambda=0$ 모드는 상수 신호가 아니다"
    $\lambda=0$ 모드의 값은 $(0.5774,\,0.5774,\,0.4082,\,0.4082)$로 평평하지 않다. 정확히는 $\sqrt d=(\sqrt2,\sqrt2,1,1)$에 비례한다. 차수로 한 번씩 나눈 판에서 잰 거칢이므로, 거칢을 0으로 만드는 신호는 상수가 아니라 **차수의 제곱근**이다. 이 벡터는 S15에서 다시 나온다. 그때는 $\sqrt{\tilde d}$다.

아래 그림에서 **네 눈금이 찍힌 가로선 한 줄**이 이 문서에서 계속 쓸 축이다. 눈금이 왼쪽에 있을수록 평평한 모드다.

<div class="gnn-fig-slot" data-gnn-fig="spec-lambda" markdown="0">
<p class="gnn-fallback">정지 프레임 2장. ① 위 표의 계산을 격자로 그린 것. 네 방향 각각에 대해 차수로 나눈 판에서 잰 거칢 0, 3, 9, 12, 크기의 제곱(모두 6), 그리고 몫 0, 0.50, 1.50, 2.00을 한 줄씩 늘어놓는다. 마지막 열이 λ와 같다. ② 0에서 2까지의 1차원 축 한 줄에 네 눈금 0, 0.50, 1.50, 2.00을 찍고, 눈금마다 그 모드의 부호 변화 횟수 0, 1, 2, 3을 붙인 것. 축 아래 한 줄: λ = 0 모드의 값은 (0.577, 0.577, 0.408, 0.408)이고 √d = (1.414, 1.414, 1, 1)에 비례한다.</p>
</div>

**축 규약 하나를 못 박는다.** 고유값 $\lambda$는 정점 축 위에 사는 양이 **아니다.** 어떤 정점의 값도 아니므로 원반 안에도, 격자의 어느 행에도 들어가지 않는다. 그래서 이 문서에서 $\lambda$는 오직 별도의 1차원 축 위에만 그린다. 반대로 고유벡터는 정점 위의 값이므로 원반과 격자에 그린다. 격자의 행 순서는 언제나 정점 순서이고 주파수 순으로 재정렬하지 않는다.

**되돌려 받는 것 하나.** [03 §2.3](03_bridge_mlp_to_gcn.md#build-operator)이 대칭 정규화를 고른 이유로 "무방향 그래프의 스펙트럼 해석이 유지된다"고 적었다. 그 문장의 뜻이 이제 채워졌다 — 행렬이 대칭이면 위와 같은 실수 고유값 네 개와 서로 수직인 모드 네 개가 항상 존재한다. 행 평균 $\tilde D^{-1}\tilde A$는 비대칭이라 이 보장이 없다.

파라미터는 아직 0개다.

## S7. 어떤 신호든 네 방향의 합이다 — 기저와 분해 { #s7 }

눈금자 넷을 얻었지만 우리 신호 $x$는 그중 어느 것도 아니다(S5의 첫 프레임). 넷으로 $x$를 표현할 수 있어야 눈금자가 쓸모가 있다.

네 모드는 서로 수직이고 길이가 1이다. 그러면 어떤 신호든 **딱 한 가지 방법으로** 네 개의 합이 된다.

$$
x=\sum_{k=1}^{4}c_ku_k,
\qquad
c_k=u_k^\top x,
\qquad
c=U^\top x.
$$

G4에서 계산한다. 예를 들어 $c_1$은

$$
c_1=0.5774(1)+0.5774(2)+0.4082(3)+0.4082(4)=1.7321+2.8577=4.5898
$$

이고, 넷을 다 하면

$$
c=(4.5898,\ -0.9856,\ -2.8167,\ -0.1691).
$$

[해설: 도출] 두 줄로 검산한다. 첫째, 방향이 서로 수직이고 길이가 1이므로 계수의 제곱합이 신호의 제곱합과 같아야 한다 — $\sum c_k^2=21.07+0.97+7.93+0.03=30$이고 $1^2+2^2+3^2+4^2=30$이다. 둘째, 각 모드를 통과시키면 크기가 $\lambda$배가 되므로 거칢은 $\sum\lambda_kc_k^2$여야 한다 — $0+0.5(0.97)+1.5(7.93)+2(0.03)=0.4857+11.9007+0.0572=12.4436$이고 이것이 S4의 12.4437이다. **S1의 뺄셈이 여기까지 왔다.**

이 표현에 이름을 붙이자. 서로 수직인 네 방향을 **기저**, 신호를 그 합으로 쓰는 것을 **분해**라 한다.

그리고 분해는 읽을거리를 준다. $x=(1,2,3,4)$는 번호순으로 나열하면 매끄러워 보이지만, G4의 실제 모양은 경로 $3\!-\!1\!-\!2\!-\!4$이고 그 경로 위에서는 $3,1,2,4$로 오르내린다. 그래서 **$x$는 부드럽지 않다** — 거칢 12.4437 중 11.9007, 곧 26.4%의 에너지가 $\lambda=1.5$ 모드에 있다.

아래 그림에서 **네 막대의 길이**가 $c_k$이고, 그 옆의 두 합계가 30과 12.4437로 맞으면 분해가 옳다.

<div class="gnn-fig-slot" data-gnn-fig="spec-basis" markdown="0">
<p class="gnn-fallback">정지 프레임 2장. ① 분해 — 왼쪽에 신호 x의 격자 (1, 2, 3, 4), 오른쪽에 네 모드의 격자 네 열, 그 사이에 계수 c = (4.590, −0.986, −2.817, −0.169)를 칸 네 개로 적은 것. 행 순서는 v1, v2, v3, v4로 고정이고 주파수 순으로 재정렬하지 않는다. ② 검산 — 계수의 제곱 (21.07, 0.97, 7.93, 0.03)과 그 합 30.00을 x의 제곱합 30과 나란히 놓고, λ를 곱한 (0.00, 0.49, 11.90, 0.06)과 그 합 12.4437을 S4의 12.4437과 나란히 놓는다. λ = 1.50 칸이 전체의 26.4%로 가장 크다.</p>
</div>

[해설: 후대 용어] 논문은 이 좌표계로 바꾸는 것, 즉 $U^\top x$를 그래프 푸리에 변환(graph Fourier transform)이라 부른다. 이름일 뿐이고 새 내용은 없다. [Kipf & Welling 2017, §2.1]

**논문 식 회수.** 방금 만든 $U^\top x$가 논문 식 (3)의 오른쪽 끝에 있는 그 $U^\top x$다. 파라미터는 아직 0개다 — 분해만 했고 아무것도 바꾸지 않았다.

## S8. 쪼개고, 곱하고, 되돌린다 — 필터와 스펙트럼 합성곱 { #s8 }

쪼개 놓았는데 아직 아무 일도 하지 않았다. 학습할 것이 하나도 없다. 무언가를 바꿔야 한다.

가장 단순한 조작을 고른다. **모드마다 정해진 수를 곱하고, 되돌린다.** 정점 축에서는 "누구와 누구를 섞을까"를 정해야 하지만, 이 좌표계에서는 네 숫자를 각각 키우거나 줄이기만 하면 된다.

$g(\lambda)=1-\lambda$를 써 보자. 배수는 $\lambda=0,0.5,1.5,2$에서 각각 $1,\,0.5,\,-0.5,\,-1$이다.

$$
c=(4.5898,\ -0.9856,\ -2.8167,\ -0.1691)
\ \longmapsto\
(4.5898,\ -0.4928,\ 1.4084,\ 0.1691).
$$

되돌리면(각 모드에 새 계수를 곱해 더하면)

$$
Ug(\Lambda)U^\top x=(3.1213,\ 3.3284,\ 0.7071,\ 1.4142).
$$

[해설: 도출] 이 네 숫자를 정점 쪽에서 한 줄로 확인할 수 있다. $g(\lambda)=1-\lambda$이므로 $I-L=S$이고, $Sx$의 1행은

$$
0.5(2)+0.7071(3)=1+2.1213=3.1213
$$

이다. **같다.** 2행은 $0.5(1)+0.7071(4)=3.3284$, 3행은 $0.7071(1)=0.7071$, 4행은 $0.7071(2)=1.4142$다. 네 줄 모두 같다. 그러니까 **주파수에서 숫자를 곱하는 일이 정점에서 이웃을 섞는 일과 같은 일**이다. 이 한 줄이 이 문서 전체의 축이다.

배수를 다르게 두면 다른 연산자가 나온다. $g=(1,0,0,0)$, 곧 가장 평평한 모드만 남기면

$$
(2.6504,\ 2.6504,\ 1.8738,\ 1.8738)
$$

이고, 이것은 $\sqrt d=(1.4142,1.4142,1,1)$에 정확히 비례한다. 모든 모드를 하나만 빼고 지우면 남는 것은 차수뿐이다. 이 장면은 S15에서 다시 나온다.

이 세 단계 전체에 이름을 붙이자. 배수표 $g_\theta$를 **필터**라 하고, 쪼개고 곱하고 되돌리는 연산 전체를 **스펙트럼 합성곱**(spectral graph convolution)이라 하며 $\star$로 쓴다. $\star$는 세 단계 전체의 이름표이지 따로 정의된 곱셈이 아니다.

[원문] 논문은 이것을 유도하지 않는다. **정의한다.** "우리는 그래프 위의 스펙트럼 합성곱을 신호 $x\in\mathbb R^N$(정점마다 스칼라 하나)과 푸리에 영역에서 $\theta\in\mathbb R^N$로 파라미터화된 필터 $g_\theta=\operatorname{diag}(\theta)$의 곱으로 정의한다"고 적고 $g_\theta\star x=Ug_\theta U^\top x$를 적는다. [Kipf & Welling 2017, §2.1, Eq. (3)]

아래 그림에서 **두 경로의 끝 격자가 같은 네 숫자**면 위 [해설: 도출]이 참이다.

<div class="gnn-fig-slot" data-gnn-fig="spec-filter" markdown="0">
<p class="gnn-fallback">정지 프레임 2장. ① 등가성 — 위쪽 경로는 세 칸이다. x를 계수 (4.590, −0.986, −2.817, −0.169)로 쪼개고, 배수 (1.00, 0.50, −0.50, −1.00)을 곱해 (4.590, −0.493, 1.408, 0.169)를 얻고, 되돌려 (3.121, 3.328, 0.707, 1.414)를 얻는다. 아래쪽 경로는 한 칸이다. 정점 위에서 계수 0.50과 0.707로 이웃을 섞어 같은 (3.121, 3.328, 0.707, 1.414)를 얻는다. 두 끝 격자가 같은 값이다. ② 배수표를 바꾸면 — 배수를 (1, 0, 0, 0)으로 두면 결과는 (2.650, 2.650, 1.874, 1.874)이고 이 네 값의 비는 √2 : √2 : 1 : 1로 √d와 같다. 배수표 네 칸이 이 연산의 자유도 전부다.</p>
</div>

**논문 식 회수.** 방금 만든 것이 논문의 식 (3)이다. **파라미터 개수는 4개, 곧 정점 수 $N$이다** — 배수표의 칸이 곧 자유도다. 여기서부터 이 숫자를 줄여 가는 것이 남은 이야기 전부다. 사슬은 $4\to K{+}1\to2\to1\to C\times F$다.

## S9. 고유벡터 행렬을 지운다 — 다항식 필터 { #s9 }

파라미터가 정점 수만큼 있다는 것은 세 가지를 뜻한다. 첫째, 정점이 백만 개인 그래프에서는 파라미터가 백만 개다. 둘째, 쓰려면 $U$를 곱해야 하는데 조밀 행렬 곱이라 $O(N^2)$이고, 애초에 $U$를 구하는 고유분해 자체가 큰 그래프에서는 감당하기 어렵다. 셋째가 가장 아프다 — **$U$는 그래프마다 다르다.** 배수표는 "이 그래프의 $k$번째 모드"에 붙어 있으므로, 학습한 필터를 다른 그래프로 가져갈 수 없다.

셋을 한꺼번에 푸는 관찰이 하나 있다.

$$
(U\Lambda U^\top)^2=U\Lambda(U^\top U)\Lambda U^\top=U\Lambda^2U^\top,
$$

$U^\top U=I$이기 때문이다. 같은 계산을 반복하면 모든 거듭제곱에 대해 $(U\Lambda U^\top)^k=U\Lambda^kU^\top$이다. 그러면 배수표를 **$\lambda$의 다항식**으로 제한했을 때

$$
Ug(\Lambda)U^\top=g(L)
\qquad\text{단, } g\text{가 다항식일 때}
$$

가 된다. 즉 **$\lambda$에 다항식을 먹이는 것과 행렬 $L$에 같은 다항식을 먹이는 것이 같은 일**이고, 오른쪽에는 $U$가 없다.

G4에서 확인한다. $p(\lambda)=1-\lambda$면 $p(L)=I-L=S$이고, $Sx$는 S8에서 이미 계산한 $(3.1213,\,3.3284,\,0.7071,\,1.4142)$다. **고유분해를 한 번도 하지 않았다.** 그리고 $S$는 간선이 있는 자리에만 값이 있으므로 희소 연산으로 간선 수에 선형인 비용에 계산된다.

이 제한에 이름을 붙이자: **다항식 필터**다. 배수표 $N$칸 대신 다항식의 계수 $K+1$개가 자유도가 된다.

[원문] 논문은 이 한 줄을 괄호 안에 적는다. "$\tilde L=\frac{2}{\lambda_{max}}L-I_N$이며, 이는 $(U\Lambda U^\top)^k=U\Lambda^kU^\top$임을 확인하면 쉽게 검증된다." 그리고 근사의 출처로 Hammond et al. (2011)을, 이 국소 합성곱으로 그래프 위 CNN을 정의한 연구로 Defferrard et al. (2016)을 든다. [Kipf & Welling 2017, §2.1, Eq. (5)]

**논문 식 회수.** 방금 만든 다리가 식 (4)에서 식 (5)로 건너가는 유일한 근거다. 파라미터는 $4\to K{+}1$로 줄었다. 아직 "어떤 다항식이냐"는 정하지 않았다 — 그것이 S11이다.

## S10. K차 다항식은 K-hop 밖을 보지 않는다 { #s10 }

다항식으로 바꾸긴 했는데 차수 $K$를 몇으로 둘지 고를 근거가 없다. $K$가 무엇을 정하는지부터 알아야 한다.

$L=I-S$이고 $S$는 간선이 있는 자리에만 값이 있다. 그러면 $L^k$의 $(i,j)$ 성분은 $i$에서 $j$까지 **길이 $k$ 이하의 걸음**을 따라 계수를 곱해 더한 것이다. 걸음이 없으면 0이다.

G4에서 $v_3$과 $v_4$ 사이의 거리는 3이다(경로 $3\!-\!1\!-\!2\!-\!4$). 그래서

$$
(L^2)_{34}=0,
\qquad
(L^3)_{34}=-0.25.
$$

[해설: 도출] $L^3=I-3S+3S^2-S^3$이고 앞의 세 항은 $(3,4)$ 자리에서 모두 0이다. 남는 것은 $-(S^3)_{34}$뿐이고, $v_3\to v_1\to v_2\to v_4$ 한 경로만 존재하므로 $(S^3)_{34}=0.7071\times0.5\times0.7071=0.25$다. 따라서 $-0.25$다.

이 성질에 이름을 붙이자: **$K$-hop 국소성**이다. $K$차 다항식 필터는 중심에서 최대 $K$걸음 떨어진 정점만 본다. 그러니까 $K$는 "한 층이 얼마나 멀리 보는가"를 정하는 숫자다.

아래 그림에서 **테 안에 들어온 정점과 값이 0이 아닌 칸이 같은 집합**이면 국소성이 성립한 것이다.

<div class="gnn-fig-slot" data-gnn-fig="spec-hop" markdown="0">
<p class="gnn-fallback">정지 프레임 3장. 중심은 v3이고 k = 1, 2, 3이다. 각 프레임에서 G4 위에 k걸음 이내 정점을 감싸는 테를 그리고, 그 옆에 (L^k)의 3행을 격자 한 줄로 적는다. k = 1은 테 안이 {v3, v1}이고 3행은 (−0.707, 0, 1, 0)이라 v2, v4 칸이 0이다. k = 2는 테 안이 {v3, v1, v2}이고 3행의 v4 칸이 여전히 0이다. k = 3에서 처음으로 테가 네 정점을 다 덮고 v4 칸이 −0.250으로 채워진다. 테 밖 칸이 0이 아닌 프레임은 하나도 없다.</p>
</div>

이것이 스펙트럼 세계와 [01 §5](01_gnn_gentle_guide.md#message-passing)의 메시지 패싱 세계를 잇는 다리다. $K$층을 쌓는 것과 $K$차 다항식을 쓰는 것이 같은 범위를 본다 — [03 §6](03_bridge_mlp_to_gcn.md#receptive-field)의 수용 집합 테가 바로 위 그림의 테다.

**논문 식 회수.** 식 (5) 바로 아래 문장이다.

[원문] "이 식은 라플라시안의 $K$차 다항식이므로 이제 $K$-국소적이다. 즉 중심 정점에서 최대 $K$걸음 떨어진 정점에만 의존한다($K$차 이웃). 식 (5)를 평가하는 복잡도는 $O(\lvert E\rvert)$, 곧 간선 수에 선형이다." [Kipf & Welling 2017, §2.1, Eq. (5)]

파라미터는 $K+1$개 그대로다.

## S11. 기성품 다항식과 눈금 다시 매기기 — Chebyshev { #s11 }

다항식이면 아무거나 좋은가. 아니다. $\lambda$가 $[0,2]$에서 움직이는데 $\lambda^5$ 같은 항을 그냥 쓰면 항끼리 크기 차이가 커져 계수가 불안정해진다. 논문은 다항식을 직접 고르지 않고 **기성품 한 벌**을 쓴다.

$$
T_0(y)=1,
\qquad
T_1(y)=y,
\qquad
T_k(y)=2yT_{k-1}(y)-T_{k-2}(y).
$$

세 줄이 전부다. 이 다항식 벌을 **Chebyshev 다항식**이라 한다. 조건이 하나 있다 — $y$가 $[-1,1]$ 안에 있을 때만 얌전하다. 그런데 우리 $\lambda$는 $[0,2]$에 있다. 그래서 눈금을 다시 매긴다.

$$
\tilde L=\frac{2}{\lambda_{max}}L-I_N,
\qquad
\lambda_{max}=\text{$L$의 가장 큰 고유값}.
$$

이 조작을 **재척도**라 한다. $\lambda=0$은 $-1$로, $\lambda=\lambda_{max}$는 $+1$로 간다.

!!! warning "이 물결은 03의 물결과 뜻이 다르다"
    [03 §2.2](03_bridge_mlp_to_gcn.md#build-operator)의 $\tilde A$에서 물결은 **자기 연결을 더했다**는 뜻이었다. 여기 $\tilde L$의 물결은 그 뜻이 아니다. 자기 연결과는 아무 관계가 없고, **고유값을 $[-1,1]$ 안으로 눌러 넣었다**는 뜻이다. 논문이 두 자리에 같은 기호를 썼으므로 이 사이트도 그대로 받아 적지만, 두 물결은 다른 조작이다. 자기 연결 쪽 물결은 S14에서 다시 나온다.

G4에서 $\lambda_{max}=2$이므로 $2/\lambda_{max}=1$이고

$$
\tilde L=L-I=-S,
\qquad
\text{눈금 } (0,\ 0.5,\ 1.5,\ 2)\ \longrightarrow\ (-1,\ -0.5,\ 0.5,\ 1).
$$

$T_2$를 실제로 만들어 본다. $T_2(y)=2y^2-1$이므로 $T_2(\tilde L)=2S^2-I$다.

$$
T_2(\tilde L)_{11}=T_2(\tilde L)_{22}=0.5,
\qquad
T_2(\tilde L)_{33}=T_2(\tilde L)_{44}=0,
$$

$$
T_2(\tilde L)_{14}=T_2(\tilde L)_{23}=0.7071,
\qquad
T_2(\tilde L)_{12}=T_2(\tilde L)_{13}=T_2(\tilde L)_{24}=\mathbf 0.
$$

[해설: 도출] $(S^2)_{11}=S_{12}^2+S_{13}^2=0.25+0.5=0.75$이므로 $2(0.75)-1=0.5$다. 반면 $(S^2)_{12}=S_{13}S_{32}+S_{14}S_{42}=0+0=0$이므로 $(1,2)$ 성분은 0이다.

**마지막 줄을 미리 밝힌다.** $T_2$는 1-hop으로 이어진 세 쌍 $(1,2),(1,3),(2,4)$에서 정확히 0이다. 그림이 고장난 것이 아니다. $T_2$는 짝수 차수 항만 갖는데 G4에는 홀수 길이 닫힌 걸음이 없어서(정점을 $\{v_1,v_2\}$와 $\{v_3,v_4\}$로 두 쪽으로 가르면 간선이 전부 쪽을 건넌다) 짝수 걸음은 홀수 거리에 도달하지 못한다. $K$-hop 국소성은 "$K$걸음 **이내**"를 말하는 것이지 "$K$걸음 전부"를 말하는 것이 아니다.

아래 그림에서 **간선 위 숫자가 0인 자리**를 세어 보면 위 문단이 확인된다.

<div class="gnn-fig-slot" data-gnn-fig="spec-cheby" markdown="0">
<p class="gnn-fallback">정지 프레임 3장. 세 프레임 모두 G4 위에 자기 고리와 간선의 계수를 숫자로 적고, 선 굵기를 계수 크기에 맞춘다. ① T₀(L̃) = I — 자기 고리가 전부 1.000이고 간선은 전부 0. ② T₁(L̃) = L̃ = −S — 자기 고리가 전부 0이고 간선은 (1,2)에서 −0.500, (1,3)과 (2,4)에서 −0.707. ③ T₂(L̃) = 2S² − I — 자기 고리가 v1, v2에서 0.500, v3, v4에서 0.000이고, 1-hop 간선 세 개가 전부 0.000이며 대신 2-hop 쌍 (1,4)와 (2,3)에 0.707이 붙는다. 그래서 ③에는 그래프에 없던 점선 두 개가 등장한다. 세 프레임 아래 공통으로 1차원 축 한 줄에 재척도된 눈금 −1.00, −0.50, 0.50, 1.00을 찍는다.</p>
</div>

**G4로는 볼 수 없는 것이 있다.** $\lambda_{max}=2$는 G4에서 **정확히** 성립한다(양쪽으로 갈리는 그래프에서만 그렇고, G4는 경로 그래프라 그렇다). 그래서 G4만 보면 재척도가 아무 일도 하지 않는 것처럼 보인다. 최소 확장 **G4○** — G4에 정점 $v_5$와 간선 $(3,5),(4,5)$를 더해 다섯 정점 고리를 만든 것 — 에서는 $\lambda_{max}=1.809$이고 $2/\lambda_{max}=1.1056$이다. 그러니까 뒤에 나올 식은 $L-I$가 아니라 $1.106\,L-I$에서 온 근사다. 이 확장은 여기 한 줄에만 쓰고 계보를 늘리지 않는다. 이 자리가 [02 §4.5 표](02_kipf2017_gcn_guide.md#spectral-choices)의 **(c)** 다.

**논문 식 회수.** 방금 만든 것이 식 (4)와 식 (5)의 $T_k$와 $\tilde\Lambda$, $\tilde L$이다.

[원문] "Chebyshev 다항식은 $T_k(x)=2xT_{k-1}(x)-T_{k-2}(x)$로 재귀적으로 정의되며 $T_0(x)=1$, $T_1(x)=x$이다. … 재척도된 $\tilde\Lambda=\frac{2}{\lambda_{max}}\Lambda-I_N$이고 $\lambda_{max}$는 $L$의 가장 큰 고유값이다. $\theta'$는 이제 Chebyshev 계수 벡터다." [Kipf & Welling 2017, §2.1, Eq. (4)]

파라미터는 $K+1$개다.

## S12. 선택 (a) — 1차식으로 자른다 { #s12 }

$K+1$개도 층마다 늘어난다. 그리고 $K$를 키우면 한 층이 멀리까지 가는데, 그 대신 층을 여러 개 쌓아도 같은 거리에 닿는다.

$K=1$로 자른다. 그러면 항이 둘뿐이다.

$$
g_{\theta'}\star x
\approx\theta'_0T_0(\tilde L)x+\theta'_1T_1(\tilde L)x
=\theta'_0x+\theta'_1\tilde Lx
=\theta'_0x+\theta'_1(L-I_N)x
=\theta'_0x-\theta'_1Sx.
$$

가운데 두 등호가 G4(또는 $\lambda_{max}=2$인 모든 그래프)에서만 성립한다 — S11의 재척도가 항등이 되기 때문이다. 이 자리가 [02 §4.5 표](02_kipf2017_gcn_guide.md#spectral-choices)의 **(a)** 다.

!!! note "$\theta'$의 프라임은 도함수가 아니다"
    $\theta'$는 $\theta$의 도함수가 아니다. 식 (3)의 배수표 $\theta\in\mathbb R^N$과 식 (4)의 Chebyshev 계수 $\theta'\in\mathbb R^K$는 개수도 뜻도 다른 물건이라, 논문이 둘을 구별하려고 붙인 표시일 뿐이다. 그리고 식 (7)에서 프라임이 다시 사라지는 것은 S13에서 둘을 하나로 묶기 때문이다.

논문은 이 선택으로 잃는 것만 있는 게 아니라고 적고, 되찾는 것 셋을 든다.

[원문] "이렇게 하면 그런 층을 여러 개 쌓아 여전히 풍부한 합성곱 필터 함수의 부류를 복원할 수 있고, Chebyshev 다항식이 주는 명시적 파라미터화에 묶이지 않는다." 둘째로 차수 분포가 매우 넓은 그래프(소셜 네트워크, 인용 네트워크, 지식 그래프)에서 국소 이웃 구조에 과적합하는 문제를 완화하리라 기대한다고 적고, 셋째로 "계산 예산이 고정되어 있을 때 이 층별 선형 형태는 더 깊은 모델을 세울 수 있게 한다"고 적는다. [Kipf & Welling 2017, §2.2]

**논문 식 회수.** 방금 만든 것이 논문의 식 (6)이다. 파라미터는 $K+1\to$ **2개**($\theta'_0$과 $\theta'_1$)로 줄었다.

## S13. 선택 (b) — 둘을 하나로 묶고, 값이 부푼다 { #s13 }

층마다 둘도 줄이고 싶다. 논문은 두 자유 숫자를 하나로 묶는다.

$$
\theta=\theta'_0=-\theta'_1
\qquad\Longrightarrow\qquad
g_\theta\star x\approx\theta\left(I_N+S\right)x.
$$

이 자리가 [02 §4.5 표](02_kipf2017_gcn_guide.md#spectral-choices)의 **(b)** 다. 이제 연산자가 완전히 정해졌으므로 계수를 전부 적을 수 있다.

| 자리 | $I+S$의 계수 |
|---|---:|
| 자기 고리 $v_1,v_2,v_3,v_4$ | $1.0000$ (넷 다) |
| 간선 $(1,2)$ | $0.5000$ |
| 간선 $(1,3)$, $(2,4)$ | $0.7071$ |

$$
(I+S)x=(4.121,\ 5.328,\ 3.707,\ 5.414).
$$

한 번은 멀쩡하다. 그런데 층을 쌓는다는 것은 이 연산자를 반복한다는 뜻이다.

$$
(I+S)^4x=(40.29,\ 44.36,\ 27.20,\ 32.96),
\qquad
(I+S)^8x=(668.1,\ 688.7,\ 465.1,\ 494.3).
$$

[해설: 도출] 왜 부푸는지는 S7의 분해가 그대로 답한다. $I+S=2I-L$이므로 이 연산자는 **$L$과 고유벡터 집합이 완전히 같고, 눈금만 $\lambda\mapsto2-\lambda$로 뒤집힌다.** 그래서 $\operatorname{spec}(I+S)=\{0,\,0.5,\,1.5,\,2\}$로 숫자 집합까지 같다. 반복하면 각 모드가 $(2-\lambda)^k$배가 되므로, 가장 큰 눈금 2를 가진 모드는 $k=8$에서 $2^8=256$배가 된다. $c_1=4.5898$에 256을 곱하면 1175이고, 그 대부분이 위 네 숫자다.

[원문] "$I_N+D^{-1/2}AD^{-1/2}$는 이제 $[0,2]$ 범위의 고유값을 갖는다는 점에 유의하라. 따라서 이 연산자를 깊은 신경망 모델에서 반복 적용하면 수치 불안정과 gradient 폭발/소실로 이어질 수 있다." [Kipf & Welling 2017, §2.2]

아래 그림에서 **오른쪽 축의 두 줄이 같은 자리에 찍히는지**를 보면, $I+S$가 $L$과 기저를 공유한다는 위 도출을 눈으로 확인할 수 있다.

<div class="gnn-fig-slot" data-gnn-fig="spec-explode" markdown="0">
<p class="gnn-fallback">정지 프레임 2장. ① 반복 — G4 위에 I + S의 계수를 적은 그림(자기 고리 넷 다 1.000, 간선 0.500 / 0.707 / 0.707)과 그 오른쪽에 k = 0, 1, 4, 8의 결과 격자 네 개. (1, 2, 3, 4) → (4.12, 5.33, 3.71, 5.41) → (40.3, 44.4, 27.2, 33.0) → (668, 689, 465, 494)로 자릿수가 늘어난다. ② 눈금 — 1차원 축 한 줄에 두 행을 겹친다. 위 행은 L의 눈금 0, 0.50, 1.50, 2.00, 아래 행은 I + S의 눈금 0, 0.50, 1.50, 2.00이다. 네 눈금 위치가 같고, 다만 가장 평평한 모드가 L에서는 왼쪽 끝 0에, I + S에서는 오른쪽 끝 2에 있다. 축 오른쪽 끝에 2⁸ = 256을 적는다.</p>
</div>

**논문 식 회수.** 방금 만든 것이 논문의 식 (7)이다. 파라미터는 $2\to$ **1개**($\theta$)로 줄었다.

## S14. 선택 (d) — 차수를 다시 센다 { #s14 }

256배를 고쳐야 한다. 무엇이 눈금 2를 만들었는가. $I+S$의 자기 고리 계수가 넷 다 **1.000**이라는 것이다 — 이웃 계수는 차수로 나눠 눌러 놓고 자기 자신만 누르지 않았다.

그러면 자기 고리도 같이 누르면 된다. 방법은 하나다. **자기 연결을 $A$ 안에 넣고 차수를 다시 센다.**

$$
\tilde A=A+I_N,
\qquad
\tilde d_i=\sum_j\tilde A_{ij},
\qquad
\hat A=\tilde D^{-1/2}\tilde A\tilde D^{-1/2}.
$$

!!! warning "이 물결은 S11의 물결과 뜻이 다르다"
    여기 $\tilde A$와 $\tilde D$의 물결은 **자기 연결을 더했다**는 뜻이다. S11의 $\tilde L$이 뜻한 "고유값을 $[-1,1]$로 눌러 넣었다"와는 무관하다. 논문이 같은 기호를 두 뜻으로 쓴 두 자리가 이 문서 안에 다 나왔다.

G4에서 차수가 $d=(2,2,1,1)$에서 $\tilde d=(3,3,2,2)$로 바뀐다. 계수를 나란히 놓는다.

| 자리 | $I+S$ | $\hat A$ |
|---|---:|---:|
| 자기 고리 $v_1$, $v_2$ | $1.0000$ | $1/3=0.3333$ |
| 자기 고리 $v_3$, $v_4$ | $1.0000$ | $1/2=0.5000$ |
| 간선 $(1,2)$ | $0.5000$ | $1/3=0.3333$ |
| 간선 $(1,3)$, $(2,4)$ | $0.7071$ | $1/\sqrt6=0.4082$ |

**결정적 한 칸은 마지막 두 줄의 비교다.** $v_1$이 자기 자신에게 주는 계수와 $v_3$에게 주는 계수의 비가 $1.0000/0.7071=1.414$에서 $0.3333/0.4082=0.8165$로 **뒤집힌다.** 왼쪽에서는 자기 자신이 이웃보다 무겁고, 오른쪽에서는 가볍다. 자기 고리만 줄어든 것이 아니라 간선 계수도 함께 바뀌었다.

$$
\hat Ax=(2.2247,\ 2.6330,\ 1.9082,\ 2.8165).
$$

이 네 숫자는 새 숫자가 아니다. [02 §6](02_kipf2017_gcn_guide.md#hand-calc)의 손계산이 얻은 값이고, [03 §2.3](03_bridge_mlp_to_gcn.md#build-operator)이 구성으로 만든 그 $\hat A$다. **두 경로가 여기서 합류한다** — 03은 "자기 자신을 포함하고 차수를 조절한다"에서 걸어왔고, 05는 간선 하나의 뺄셈에서 걸어왔는데 같은 행렬에 도착했다.

[해설] 논문이 쓴 단어는 **치환**(renormalization trick)이다. $\hat A$는 $I+S$를 정리한 것이 아니라 **다른 연산자로 바꾼 것**이다. 그리고 논문은 이 화살표에 식 번호를 붙이지 않았다 — §2.2의 번호 없는 본문 한 줄이다. 식 (7)과 식 (8) 사이에 유도가 없다는 사실 자체가, 이것이 유도가 아니라 교체라는 표시다.

아래 그림에서 **왼쪽과 오른쪽에서 가장 굵은 선이 서로 다른 선**이면 비가 뒤집힌 것이다.

<div class="gnn-fig-slot" data-gnn-fig="spec-swap" markdown="0">
<p class="gnn-fallback">정지 프레임 2장. 두 프레임 다 같은 G4 배선 위에 자기 고리와 간선의 계수를 숫자로 적고 선 굵기를 계수에 맞춘다. ① I + S — 자기 고리가 v1..v4 전부 1.000으로 가장 굵고, 간선은 (1,2)가 0.500, (1,3)과 (2,4)가 0.707이다. 차수는 d = (2, 2, 1, 1)이다. ② Â — 자기 고리가 v1, v2에서 0.333, v3, v4에서 0.500이고, 간선은 (1,2)가 0.333, (1,3)과 (2,4)가 0.408이다. 차수는 d̃ = (3, 3, 2, 2)이다. ①에서는 v1의 자기 고리가 간선 (1,3)보다 굵고, ②에서는 간선 (1,3)이 자기 고리보다 굵다. 두 프레임 아래에 비 1.414와 0.817을 적는다. 배선은 한 선도 바뀌지 않았고 숫자만 바뀌었다.</p>
</div>

**논문 식 회수.** 이것이 논문의 식 (8)이고, 다채널로 쓰면

$$
Z=\tilde D^{-1/2}\tilde A\tilde D^{-1/2}X\Theta,
\qquad
\Theta\in\mathbb R^{C\times F}.
$$

여기서 $\Theta$는 식 (2)의 $W^{(l)}$과 **같은 물건**이다. 그러니까 파라미터는 $1\to$ **$C\times F$개**로 늘었다 — 사슬 $4\to K{+}1\to2\to1\to C\times F$의 마지막 칸이다. 줄어들기만 한 것이 아니라, 정점 축에서 뺀 자유도를 특성 축에 다시 넣은 것이다. 그리고 이 $C\times F$는 $N$에 의존하지 않는다.

## S15. 대가 — 무엇을 내주었나 { #s15 }

폭발은 멎었다. 무엇을 내주었는지를 세 가지로 나눠 확인한다.

**첫째, 가장 큰 눈금이 정확히 1이 된다.**

[해설: 도출] $\sqrt{\tilde d}=(\sqrt3,\sqrt3,\sqrt2,\sqrt2)$를 $\hat A$에 넣어 본다. 1행은

$$
\tfrac13\sqrt3+\tfrac13\sqrt3+\tfrac1{\sqrt6}\sqrt2
=0.5774+0.5774+0.5774=1.7321=\sqrt3
$$

이다. 배수가 정확히 1이다. 즉 $\hat A\sqrt{\tilde d}=\sqrt{\tilde d}$이고, 이 방향은 반복해도 전혀 줄지도 늘지도 않는다. S6의 $\sqrt d$가 여기서 $\sqrt{\tilde d}$가 되어 돌아왔다.

**둘째, 없던 음수 눈금이 생긴다.**

$$
\operatorname{spec}(\hat A)=\{-0.2287,\ 0.1667,\ 0.7287,\ 1\}.
$$

$I+S$의 가장 작은 눈금은 0이었다. 교체 뒤에는 $-0.2287$이다. 부호가 바뀌는 모드가 새로 생겼다는 뜻이다.

**셋째, 1이 아닌 모든 모드가 $\lvert\mu\rvert^k$로 죽는다.** $\hat A$도 대칭이므로 자기 자신의 모드 네 개 $w_1,\dots,w_4$와 눈금 $\mu_1,\dots,\mu_4$를 갖는다. $\hat Aw=\mu w$이면 $\hat A^kw=\mu^kw$이므로

$$
\hat A^kx=\sum_{j}(w_j^\top x)\,\mu_j^k\,w_j.
$$

$\mu=1$인 항만 남고 나머지는 지수적으로 사라진다. $0.7287^8=0.0795$이므로 여덟 층이면 둘째로 큰 모드도 8%만 남는다. 그래서

$$
\hat A^8x=(2.588,\ 2.641,\ 2.088,\ 2.182)
\qquad\text{대}\qquad
(I+S)^8x=(668.1,\ 688.7,\ 465.1,\ 494.3).
$$

[02 §11](02_kipf2017_gcn_guide.md#viz-oversmoothing)이 재는 행 간 거리 $0.439\to0.236\to0.067$의 비가 0.733과 0.730인데, 이것이 위의 0.7287이다. 같은 계산의 앞뒤다.

**그리고 반증.** $\hat A$는 식 (3)의 어떤 필터로도 쓸 수 없다. 식 (3)의 필터는 $L$의 기저를 고정해 놓고 눈금만 바꾸는 물건이므로, $\hat A$가 그런 필터이려면 $\hat A$의 모드가 $L$의 모드여야 한다. 확인해 보자.

[해설: 도출] $\hat A$의 $\mu=1$ 모드는 $\sqrt{\tilde d}$다. 이것을 S7의 방법으로 $L$의 네 모드에 분해한다.

$$
u_1^\top\sqrt{\tilde d}=3.1547,
\qquad
u_3^\top\sqrt{\tilde d}=-0.2188,
\qquad
u_2^\top\sqrt{\tilde d}=u_4^\top\sqrt{\tilde d}=0.
$$

성분이 **둘**이다(크기를 1로 맞추면 0.998과 0.069). 하나가 아니므로 $\sqrt{\tilde d}$는 $L$의 고유벡터가 아니다. 따라서 $\hat A$는 $Ug(\Lambda)U^\top$ 꼴이 아니고, **동치가 아니다.**

여기서 흔히 어긋나는 것 하나를 못 박는다. 동치를 깨는 것은 고유값 **범위**가 아니다. S13에서 본 대로 $I+S$는 $L$과 고유값 범위도 같고 기저도 같은데 스펙트럼 필터다. 스펙트럼은 $g$를 어떻게 고르든 무엇이든 될 수 있다. 동치를 깨는 것은 오직 **기저가 바뀌었다**는 것이고, 기저가 실제로 바뀐 자리는 이 사슬에서 S14 한 곳뿐이다. 이유도 분명하다 — $\hat A$의 모드는 원래 그래프 $G$가 아니라 자기 연결을 넣은 그래프 $\tilde G$에 붙어 있다.

아래 그림에서 **오른쪽 막대가 하나가 아니라 둘**이면 반증이 성립한 것이다.

<div class="gnn-fig-slot" data-gnn-fig="spec-mu" markdown="0">
<p class="gnn-fallback">정지 프레임 3장. ① 눈금 — 1차원 축 한 줄에 두 행을 겹친다. 위 행은 I + S의 눈금 0, 0.50, 1.50, 2.00이고, 아래 행은 Â의 눈금 −0.229, 0.167, 0.729, 1.000이다. 아래 행이 (−1, 1] 안으로 죄어져 있고 가장 큰 눈금이 정확히 1.000이며 음수 눈금이 새로 하나 생겼다. ② 반복 — Â를 k = 1, 2, 4, 8번 적용한 결과 (2.22, 2.63, 1.91, 2.82), (2.40, 2.77, 1.86, 2.48), (2.52, 2.65, 2.01, 2.27), (2.59, 2.64, 2.09, 2.18)을 격자 네 개로 늘어놓고, 같은 k = 8에서 I + S가 준 (668, 689, 465, 494)를 아래에 나란히 적는다. 축 옆에 0.729의 8제곱이 0.0795임을 적는다. ③ 반증 — 왼쪽에 Â의 μ = 1.000 모드인 √d̃ = (1.732, 1.732, 1.414, 1.414)를 격자로, 오른쪽에 그것을 L의 네 모드로 분해한 막대 네 개 3.155, 0.000, −0.219, 0.000을 그린다. 0이 아닌 막대가 둘이다. 하나뿐이어야 반증이 실패한다.</p>
</div>

**논문 식 회수.** 여기서 회수할 것이 가장 많다. 이 사슬의 네 선택 지점 (a)(b)(c)(d)를 한 표로 요약한 것이 [02 §4.5](02_kipf2017_gcn_guide.md#spectral-choices)이고, 그 네 지점을 논문이 실제로 실험한 결과가 [02 §9.3의 Table 3](02_kipf2017_gcn_guide.md#propagation-comparison)이다. 즉 식 (7)에서 식 (8)로 가는 화살표를 정당화한 것은 유도가 아니라 표 한 줄의 차이다.

[원문] "우리는 나아가 제안한 renormalize된 전파 모델(식 (8))이 소박한 1차 모델에 비해 효율(파라미터와 곱셈·덧셈 같은 연산의 수가 더 적다)과 여러 데이터셋에서의 예측 성능을 **둘 다** 개선함을 보였다." [Kipf & Welling 2017, §7.1]

셋째 대가는 곧장 깊이 이야기가 된다 — [02 §11](02_kipf2017_gcn_guide.md#depth-appendix-b)의 Figure 5와 residual 식 (14), [02 §13](02_kipf2017_gcn_guide.md#misconceptions)의 "$\hat A$는 안전하다"는 오해가 모두 위의 $\lvert\mu\rvert^k$ 한 줄 위에 서 있다. 파라미터는 $C\times F$개다. 사슬이 닫혔다.

## 종점: 독자가 쓸 수 있어야 하는 두 문장 { #two-sentences }

이 문서를 덮고 나면 다음 두 문장을 종이에 다시 쓸 수 있어야 한다. 숫자는 G4 것이다.

1. **renormalization trick.** "식 (7)의 연산자는 자기 자신에게 계수 1을 주는데 이웃에게는 $1/\sqrt{d_id_j}$를 준다. 여덟 번 반복하면 668까지 부푼다. 자기 연결을 $A$ 안에 넣고 차수를 다시 세면 자기 계수도 $1/\tilde d_i$로 함께 줄어 가장 큰 고유값이 정확히 1이 되고, 여덟 번 반복해도 2.6 근처에 머문다. 대신 나머지 모드가 $\lvert\mu\rvert^k$로 죽는다."
2. **동치가 아니다.** "$\hat A$의 최대 고유벡터 $\sqrt{\tilde d}$를 $L$의 네 모드로 쪼개면 성분이 둘이다. 하나가 아니므로 $\hat A$는 $L$의 고유벡터를 그대로 쓰는 필터가 아니고, 따라서 식 (3)의 어떤 $g$로도 쓸 수 없다. 마지막 한 걸음은 근사도 정리도 아니고 교체이며, 그것을 정당화한 것은 유도가 아니라 Table 3이다."

두 문장을 쓸 수 있으면 [02](02_kipf2017_gcn_guide.md)의 §4를 논문의 순서대로 읽을 준비가 된 것이다.

## 확인문제 { #exercises }

문제 본문과 해답은 [04](04_exercises.md#solutions)의 문제 9~12에 있다. 넘어가기 전에 네 가지는 손으로 해 볼 만하다.

1. 간선 셋의 차에서 시작해 $E(x)$, $\Delta x$, $x^\top\Delta x$를 각각 계산하고 세 값이 어떻게 맞물리는지 적어라. → [04 문제 9](04_exercises.md#p9)
2. $u_3$이 $\lambda=1.5$의 고유벡터임을 $L$의 한 행으로 검산하고, 정수판으로 $\lambda$가 거칢을 크기로 나눈 값임을 확인하라. → [04 문제 10](04_exercises.md#p10)
3. $c=U^\top x$를 계산하고, $g(\lambda)=1-\lambda$를 먹인 결과가 $Sx$와 같음을 정점 쪽 한 줄로 확인하라. → [04 문제 11](04_exercises.md#p11)
4. $I+S$와 $\hat A$의 계수를 자리별로 나란히 적고, $v_1$의 자기/이웃 비가 어떻게 뒤집히는지 수로 보여라. → [04 문제 12](04_exercises.md#p12)

## 참고 { #refs }

- Kipf, T. N. and Welling, M., "Semi-Supervised Classification with Graph Convolutional Networks," ICLR 2017, arXiv:1609.02907. — 회수한 식은 (1), (3)–(8)이다.
- Hammond, D. K., Vandergheynst, P., and Gribonval, R., "Wavelets on graphs via spectral graph theory," *Applied and Computational Harmonic Analysis*, 30(2):129–150, 2011. — S9·S11의 Chebyshev 근사의 출처.
- Defferrard, M., Bresson, X., and Vandergheynst, P., "Convolutional Neural Networks on Graphs with Fast Localized Spectral Filtering," NIPS 2016. — S11에서 멈춘 모델(ChebNet). [02 §9.3 Table 3](02_kipf2017_gcn_guide.md#propagation-comparison)의 Chebyshev 두 행이 이 논문이다.
- Bruna, J., Zaremba, W., Szlam, A., and LeCun, Y., "Spectral Networks and Locally Connected Networks on Graphs," ICLR 2014. — S8에서 멈춘 형태, 곧 $\theta\in\mathbb R^N$를 그대로 학습하는 초기 모델.
- 앞: [01](01_gnn_gentle_guide.md) · [03](03_bridge_mlp_to_gcn.md). 다음: [02](02_kipf2017_gcn_guide.md) · [04](04_exercises.md).

---
---

# 부록 A. 그림 명세 (구현자용, 사이트에는 안 나감)

**먼저 — 재계산 정정 한 건.** findings §3.2 S15는 $0.7287^8=0.0806$이라고 적었는데, 재계산하면 $0.7287136^8=0.079516$이다. 본문과 그림은 **0.0795**를 쓴다(그림은 registry가 계산하므로 자동으로 맞는다). 0.0806은 $\mu=0.730$에서 나오는 값이다. findings의 나머지 수치는 전부 재계산과 일치했다: $\operatorname{spec}(\hat A)$는 $\hat A$가 $v_1\!\leftrightarrow\!v_2$, $v_3\!\leftrightarrow\!v_4$ 교환에 불변이므로 $2\times2$ 블록 둘로 쪼개져 $\{1,\,1/6\}$과 $\{0.7287136,\,-0.2287136\}$이 정확히 나오고, G4○는 5-사이클이라 $\lambda_{max}=1-\cos(4\pi/5)=1.809017$이다.

**공통 규약.** 모든 프레임은 G4, $x=(1,2,3,4)$, 좌표 동결. 격자 행 순서는 $v_1..v_4$ 영구 고정(주파수 순 재정렬 금지). 판정 문장은 SVG 텍스트가 아니라 `gnn-frame__cap`(HTML 캡션)에 둔다. 본문이 인쇄한 손계산은 그림에 두지 않고, 그림이 인쇄하는 수치는 본문에서 "그림의 ②"로만 가리킨다.

---

## A0. 새로 필요한 registry 커널

`figures.js`에 상수를 적지 않으려면 `registry.js`에 다음이 먼저 들어가야 한다. 모두 `NI3.la` 또는 `ops(graph)`에 붙인다.

| 이름 | 서명 | 쓰는 그림 |
|---|---|---|
| `madd(A,B)` | 같은 크기 행렬 합 | spec-cheby, spec-explode |
| `msub(A,B)` | 같은 크기 행렬 차 | spec-hop, spec-cheby |
| `mscale(A,s)` | 스칼라배 | spec-cheby, spec-lambda |
| `transpose(A)` | 전치 | spec-basis (`c=Uᵀx`) |
| `matvec(A,v)` | 행렬 × 벡터 → 벡터 | spec-eigen, spec-filter, spec-explode, spec-mu |
| `dot(u,v)` | 내적 | spec-basis, spec-mu (분해 계수) |
| `cheby(Ltil,K)` | `[T₀,…,T_K]` 행렬 배열을 재귀로 생성 | spec-cheby |
| `ops(g).Ltilde` | $\tilde L=(2/\lambda_{max})L-I$, `λmax`는 `jacobiEig(L)`의 마지막 값 | spec-cheby, spec-lambda |
| `ops(g).lmax` | $L$의 최대 고유값 | spec-cheby (G4○ 한 줄) |
| `G4circ` | 그래프 등록 — `nodes:[1..5]`, `edges` = G4 + `[3,5],[4,5]`, `pos`의 1~4는 G4와 동일, `x` 1~5 | spec-cheby (③ 캡션 한 줄만) |

그리고 `jacobiEig`의 부호 규약을 **"첫 비영 성분을 양수로"** 로 바꾼다(현재는 "절댓값 최대 성분을 양수로"). 이 변경은 `figSpectral`이 유일한 사용처이지만, 구현 시 `verify-figures`로 전 그림 회귀 검증을 돌려 `figLineage`·`figConverge`·`figDepth`·`figNorm`·`figSemi`가 값이 바뀌지 않음을 확인한다.

기존 `figSpectral`은 **삭제하지 않고** id를 유지한 채 02 §4.0에서 떼어내지 않는다 — 02 §4를 30행으로 줄이는 안(부록 B2)에서는 `figSpectral` 슬롯 자체가 02에서 사라지므로, 구현자는 `figSpectral`을 제거하고 아래 아홉 개로 대체한다. 프레임 ①(신호)과 ②~⑤(고유벡터)의 그리기 코드는 `spec-eigen`이 거의 그대로 재사용한다.

---

## A1. `spec-eigen` — S5

- **위치**: S5, "부호 규약" 문단 바로 뒤.
- **읽는 법 한 줄**(본문): "원반의 +/− 패턴이 간선을 따라 몇 번 바뀌는지 세어 보면, 그 횟수가 표의 λ 순서와 정확히 같다."
- **프레임 5장**, `cols: 2`, ①만 `span:'full'`.

| # | 제목 | 무엇을 그리는가 | registry 계산 | HTML 캡션 |
|---|---|---|---|---|
| ① | 우리 신호는 고유벡터가 아니다 | 원반 4 + 격자 2열($x$와 $Lx$) | `scalarX(G4)`, `matvec(ops.L, x)` | "두 열의 행 비율이 다르다. $x$는 통과해도 모양이 그대로인 신호가 아니다." |
| ② | $u_1$, λ = 0.00 | 원반 4(값+부호 기호) + 격자 1열 + 부호 바뀌는 간선 점선 | `jacobiEig(ops.L).vectors[0]` | "λ = 0.00 · 부호 변화 0회 — 가장 평평한 모드. 값은 상수가 아니라 √d에 비례한다." |
| ③ | $u_2$, λ = 0.50 | 같음 | `.vectors[1]` | "λ = 0.50 · 부호 변화 1회" |
| ④ | $u_3$, λ = 1.50 | 같음 | `.vectors[2]` | "λ = 1.50 · 부호 변화 2회" |
| ⑤ | $u_4$, λ = 2.00 | 같음 | `.vectors[3]` | "λ = 2.00 · 부호 변화 3회 — 가장 진동하는 모드. 세 간선이 모두 점선이다." |

②~⑤ 각 프레임의 격자 오른쪽에 검산 한 줄을 `note`로: `L의 {r}행 · u{k} = {값} = λ × u{k}의 {r}성분`. 행 $r$은 그 벡터에서 성분이 0이 아닌 첫 행으로 고른다.

- **폴백**: 본문 슬롯의 `<p class="gnn-fallback">` 참조(위 본문에 적힌 것이 정본).
- **falsify**: "이 넷이 정말 '모양이 안 변하는 신호'라면 각 프레임의 검산 줄 좌변과 우변이 같아야 한다. 네 프레임 모두 같다. ① 프레임에서만 두 열의 비율이 어긋난다."
- **새 커널**: `matvec`.

## A2. `spec-lambda` — S6

- **위치**: S6, "λ=0 모드는 상수 신호가 아니다" 경고 바로 뒤.
- **읽는 법 한 줄**: "네 눈금이 찍힌 가로선 한 줄이 이 문서에서 계속 쓸 축이다. 눈금이 왼쪽에 있을수록 평평한 모드다."
- **프레임 2장**, `cols: 1`, 둘 다 `span:'full'`.

| # | 제목 | 무엇을 그리는가 | registry 계산 | HTML 캡션 |
|---|---|---|---|---|
| ① | λ는 거칢을 크기로 나눈 값이다 | 격자 4행 × 4열 — 각 행이 한 모드, 열은 [$v^\top Lv$ / 크기의 제곱 / 몫 / 부호 변화 수] | `dot(v, matvec(ops.L, v))`, `dot(v,v)`, `ops.A`의 간선 목록으로 부호 변화 | "몫 열이 λ와 같다. 네 모드의 크기 제곱이 모두 같으므로 λ의 순서는 거칢의 순서다. 여기 거칢은 차수로 나눈 판에서 잰 값이다." |
| ② | 고유값은 별도 축 위에만 산다 | `drawAxis` 한 행, 0~2 구간, 눈금 4개 + 각 눈금 아래 부호 변화 수 | `jacobiEig(ops.L).values` | "λ는 어떤 정점의 값도 아니므로 원반에도 격자에도 넣지 않는다. 이 축이 S13·S15에서 그대로 다시 나온다." |

①의 정수판 표기는 숫자를 정규화 벡터에서 계산하고 표시만 $\times\sqrt6$ 한다. 상수를 적지 않는다.

- **폴백**: 본문 슬롯 참조.
- **falsify**: "λ가 거칢 눈금이 아니라면 ① 마지막 열과 ② 축의 눈금이 어긋나야 한다. 네 줄이 모두 일치한다."
- **새 커널**: `mscale`(정수판 표시용), `dot`.

## A3. `spec-basis` — S7

- **위치**: S7, "x는 부드럽지 않다" 문단 바로 뒤.
- **읽는 법 한 줄**: "네 막대의 길이가 $c_k$이고, 그 옆의 두 합계가 30과 12.4437로 맞으면 분해가 옳다."
- **프레임 2장**, `cols: 1`, 둘 다 `span:'full'`.

| # | 제목 | 무엇을 그리는가 | registry 계산 | HTML 캡션 |
|---|---|---|---|---|
| ① | 한 신호가 네 방향의 합으로 | 왼쪽 격자 1열($x$) + 가운데 칸 4개($c$) + 오른쪽 격자 4열($u_1..u_4$) | `matmul(transpose(U), colMat(x))` | "행 순서는 $v_1..v_4$로 고정이다. 주파수 순으로 재정렬하지 않는다." |
| ② | 두 합계가 맞는가 | 칸 두 줄 — 윗줄 $c_k^2$ 넷과 합, 아랫줄 $\lambda_kc_k^2$ 넷과 합. 아랫줄 오른쪽에 S4의 $x^\top Lx$ 한 칸 | 같은 `c`, `jacobiEig(ops.L).values` | "윗줄 합은 $x$의 제곱합과 같고, 아랫줄 합은 S4의 거칢과 같다. λ = 1.50 칸이 전체의 26.4%다." |

②의 26.4%는 계산값이다(`λ₃c₃²/Σλc²`).

- **폴백**: 본문 슬롯 참조.
- **falsify**: "네 방향이 서로 수직이 아니라면 ②의 두 합계 중 적어도 하나가 어긋나야 한다. 둘 다 맞는다."
- **새 커널**: `transpose`, `dot`.

## A4. `spec-filter` — S8

- **위치**: S8, [원문] 인용 바로 뒤.
- **읽는 법 한 줄**: "두 경로의 끝 격자가 같은 네 숫자면 위 [해설: 도출]이 참이다."
- **프레임 2장**, `cols: 1`, 둘 다 `span:'full'`.

| # | 제목 | 무엇을 그리는가 | registry 계산 | HTML 캡션 |
|---|---|---|---|---|
| ① | 주파수 세 칸과 정점 한 칸이 같은 곳에 닿는다 | 위 줄: 격자($x$) → 칸4($c$) → 칸4($g\!\cdot\!c$) → 격자(되돌린 값). 아래 줄: 같은 격자($x$)에서 G4 원반을 지나 격자(같은 값). 두 끝 격자를 같은 테로 묶는다 | `c=Uᵀx`, `g=1−λ`, 복원 `U(g∘c)`, 그리고 `matvec(ops.Ssym, x)` | "위 경로는 고유분해를 쓰고 아래 경로는 쓰지 않는다. 끝 네 숫자가 같다 — 주파수에서 곱하기가 정점에서 이웃 섞기다." |
| ② | 배수표를 바꾸면 무엇이 남는가 | 칸4(배수 1,0,0,0) → 격자(결과) → 그 옆에 $\sqrt d$ 격자. 두 열의 비를 칸 하나에 | 같은 경로에 `g=[1,0,0,0]`, `ops.d.map(Math.sqrt)` | "모든 모드를 하나만 빼고 지우면 남는 것은 차수뿐이다. 이 장면이 S15에서 $\sqrt{\tilde d}$로 다시 나온다." |

- **폴백**: 본문 슬롯 참조.
- **falsify**: "식 (3)이 정점 축 연산과 무관한 별개의 정의라면 ①의 두 끝 격자가 달라야 한다. 네 자리 소수까지 같다."
- **새 커널**: `transpose`, `matvec`.

## A5. `spec-hop` — S10

- **위치**: S10, "이 성질에 이름을 붙이자" 문단 바로 뒤.
- **읽는 법 한 줄**: "테 안에 들어온 정점과 값이 0이 아닌 칸이 같은 집합이면 국소성이 성립한 것이다."
- **프레임 3장**, `cols: 2`.
- 프레임 $k=1,2,3$: 왼쪽에 G4 원반 + `rings:[{members: dist≤k}]`(03 §6 `figDepth`의 테를 그대로 재사용), 오른쪽에 `(L^k)`의 3행을 격자 한 줄로. 테 밖 정점의 칸은 값 그대로 0이 찍힌다.
- **registry**: `matpow(msub(eye, ops.Ssym), k)` 또는 `ops.L`에 `matpow`. `hopDistance(G4, 3)`.
- **캡션**: $k$별로 "테 안 {n}개 · $(L^{k})_{34}$ = {값}". $k=1,2$에서 0, $k=3$에서 −0.250.
- **폴백**: 본문 슬롯 참조.
- **falsify**: "$K$차 다항식이 $K$-hop 밖을 본다면 $k=2$ 프레임의 $v_4$ 칸이 0이 아니어야 한다. 0이다. 그리고 $k=3$에서 처음으로 0이 아니게 된다."
- **새 커널**: `msub`.

## A6. `spec-cheby` — S11

- **위치**: S11, "마지막 줄을 미리 밝힌다" 문단 바로 뒤.
- **읽는 법 한 줄**: "간선 위 숫자가 0인 자리를 세어 보면 위 문단이 확인된다."
- **프레임 3장**, `cols: 2`, ③만 `span:'full'`(없던 2-hop 점선 두 개를 그려야 해서 폭이 필요하다).

| # | 제목 | 무엇을 그리는가 | registry 계산 | HTML 캡션 |
|---|---|---|---|---|
| ① | $T_0(\tilde L)=I$ | G4 원반 + 자기 고리 값 + 간선 값(굵기=계수) | `cheby(ops.Ltilde, 2)[0]` | "0차는 아무도 섞지 않는다. 자기 고리만 1.000이다." |
| ② | $T_1(\tilde L)=\tilde L$ | 같음 | `cheby(...)[1]` | "1차는 1-hop 간선에만 값을 준다. $\lambda_{max}=2$인 G4에서 $\tilde L=L-I=-S$다." |
| ③ | $T_2(\tilde L)=2\tilde L^2-I$ | 같음 + 그래프에 없는 쌍 (1,4)·(2,3)을 점선으로 추가 | `cheby(...)[2]` | "1-hop 세 쌍이 전부 0.000이고 2-hop 두 쌍에 0.707이 붙는다. G4에 홀수 길이 닫힌 걸음이 없어서 짝수 차수 항은 홀수 거리에 닿지 않는다 — 그림이 틀린 것이 아니다." |

세 프레임 공통 하단에 `drawAxis` 한 줄로 재척도 눈금 $(-1,-0.5,0.5,1)$. 축 캡션: "G4○(= G4 + $v_5$, 간선 (3,5)(4,5))에서는 $\lambda_{max}=1.809$라 $2/\lambda_{max}=1.106$이다."

- **폴백**: 본문 슬롯 참조.
- **falsify**: "'$K$차 다항식 = $K$-hop 전부'가 맞다면 ③에서 1-hop 세 간선이 0이 아니어야 한다. 셋 다 0.000이다."
- **새 커널**: `cheby`, `Ltilde`, `lmax`, `madd`, `mscale`, `G4circ`.

## A7. `spec-explode` — S13

- **위치**: S13, [원문] 인용 바로 뒤.
- **읽는 법 한 줄**: "오른쪽 축의 두 줄이 같은 자리에 찍히는지를 보면, $I+S$가 $L$과 기저를 공유한다는 위 도출을 눈으로 확인할 수 있다."
- **프레임 2장**, `cols: 1`, 둘 다 `span:'full'`.

| # | 제목 | 무엇을 그리는가 | registry 계산 | HTML 캡션 |
|---|---|---|---|---|
| ① | 한 번은 멀쩡하고 여덟 번은 아니다 | 왼쪽 G4 원반(자기 고리 1.000 넷, 간선 0.500/0.707/0.707) + 오른쪽 격자 4개($k=0,1,4,8$) | `ops.Iplus`, `matpow`, `matvec` | "계수는 하나도 1을 넘지 않는데 결과는 668이 된다. 반복이 하는 일은 곱셈이 아니라 거듭제곱이다." |
| ② | 눈금은 뒤집히기만 했다 | `drawAxis` 두 행 — 위 $L$, 아래 $I+S$. 눈금 위치가 같다. 오른쪽 끝에 $2^8=256$ | `jacobiEig(ops.L).values`, `jacobiEig(ops.Iplus).values` | "$I+S=2I-L$이므로 두 연산자의 고유벡터 집합은 **완전히 같고** 눈금만 $\lambda\mapsto2-\lambda$로 뒤집힌다. 가장 평평한 모드가 왼쪽 끝에서 오른쪽 끝으로 옮겨 갔다." |

②의 캡션은 기존 `figSpectral`의 "같은 자리에 찍히지만 대응 고유벡터는 다르다"를 **폐기하고** 위 문장으로 교체한다. 그 문장은 수학적으로 거짓이다.

- **폴백**: 본문 슬롯 참조.
- **falsify**: "$I+S$의 폭발이 계수 하나가 커서 생긴 것이라면 ①의 원반에 1을 넘는 계수가 있어야 한다. 없다. 원인은 ②의 오른쪽 끝 눈금 2.00이다."
- **새 커널**: `matvec`.

## A8. `spec-swap` — S14

- **위치**: S14, [해설] "치환" 문단 바로 뒤.
- **읽는 법 한 줄**: "왼쪽과 오른쪽에서 가장 굵은 선이 서로 다른 선이면 비가 뒤집힌 것이다."
- **프레임 2장**, `cols: 2`.

| # | 제목 | 무엇을 그리는가 | registry 계산 | HTML 캡션 |
|---|---|---|---|---|
| ① | 식 (7)의 계수 | G4 원반, 자기 고리 값 4개 + 간선 값 3개, `edgeWidth`=계수 | `ops.Iplus`, `ops.d` | "$d=(2,2,1,1)$. $v_1$의 자기 고리 1.000이 간선 (1,3) 0.707보다 굵다 — 비 1.414." |
| ② | 식 (8)의 계수 | 같은 배선, 같은 좌표, 숫자만 다름 | `ops.Ahat`, `ops.dt` | "$\tilde d=(3,3,2,2)$. $v_1$의 자기 고리 0.333이 간선 (1,3) 0.408보다 **가늘다** — 비 0.817. 배선은 한 선도 바뀌지 않았다." |

두 프레임의 `edgeWidth` 정규화 기준(`scale`)을 **같은 값**으로 고정해야 굵기 비교가 의미를 갖는다. 기준은 두 행렬 전체의 최댓값(=1.000).

- **폴백**: 본문 슬롯 참조.
- **falsify**: "renormalization이 자기 고리만 줄인 것이라면 ①과 ②의 간선 숫자가 같아야 한다. 0.500 → 0.333, 0.707 → 0.408로 전부 바뀐다."
- **새 커널**: 없음(기존 `Iplus`, `Ahat`, `edgeWidth`로 충분).

## A9. `spec-mu` — S15

- **위치**: S15, "여기서 흔히 어긋나는 것 하나를 못 박는다" 문단 바로 뒤.
- **읽는 법 한 줄**: "오른쪽 막대가 하나가 아니라 둘이면 반증이 성립한 것이다."
- **프레임 3장**, `cols: 1`, 셋 다 `span:'full'`.

| # | 제목 | 무엇을 그리는가 | registry 계산 | HTML 캡션 |
|---|---|---|---|---|
| ① | 눈금이 안쪽으로 죄어진다 | `drawAxis` 두 행 — 위 $I+S$, 아래 $\hat A$. −1~2 구간 | `jacobiEig(ops.Iplus).values`, `jacobiEig(ops.Ahat).values` | "가장 큰 눈금이 정확히 1.000이 되고, 없던 음수 눈금 −0.229가 새로 생긴다." |
| ② | 여덟 층 뒤 | 격자 4개($\hat A^kx$, $k=1,2,4,8$) + 아래에 $(I+S)^8x$ 한 줄 | `matpow(ops.Ahat,k)`, `matpow(ops.Iplus,8)` | "같은 $k=8$에서 2.6 대 668이다. 대가는 나머지 모드가 $0.729^8=0.0795$로 죽는 것이다." |
| ③ | 반증 — 성분이 둘이다 | 왼쪽 격자($\sqrt{\tilde d}$) + 오른쪽 막대 4개(그 벡터를 $L$의 모드에 분해한 계수) | `ops.dt.map(Math.sqrt)`, `dot(u_k, √d̃)` | "0이 아닌 막대가 **둘**이다(3.155와 −0.219). 하나였다면 $\hat A$는 $L$의 필터일 수 있었다. 둘이므로 아니다." |

③의 막대는 정점 축 위 양이 아니므로 격자가 아니라 **막대**(길이 = 계수, 0선 기준 좌우)로 그린다. $\lambda$ 축과 같은 취급이다.

- **폴백**: 본문 슬롯 참조.
- **falsify**(그림 전체): "'GCN은 스펙트럼 합성곱과 동치'가 맞다면 ③에서 0이 아닌 막대가 **하나**여야 한다. 둘이다. — 동치를 깨는 것은 ①의 눈금 범위가 아니라 ③의 기저다. $I+S$는 ①에서 범위가 $L$과 같은데도 스펙트럼 필터다."
- **새 커널**: `dot`, `transpose`.

> 이 falsify 문장이 기존 `figSpectral`의 falsify("세 눈금이 같은 자리에 찍혀야 한다")를 대체한다. 기존 문장은 M-12에 따라 성립하지 않는다.

---

# 부록 B. 동반 변경 (정확한 교체 텍스트)

## B1. 02 §2 — 식 (1) 아래 삽입

`02:48`의 "여기서 $\Delta=D-A$는 비정규화 그래프 라플라시안이다. [Kipf & Welling 2017, Eq. (1)]" **바로 다음 줄**에 삽입한다(기존 [원문]·[해설] 문단은 그대로 둔다).

```markdown
[해설] 이 식은 하나의 점수다. G4($E=\{(1,2),(1,3),(2,4)\}$, $x=(1,2,3,4)$)에서 간선마다 양 끝 값의 차는 $-1,-2,-2$이고, 제곱해 더하면 $1+4+4=9$다. 같은 9가 우변에서도 나온다 — $\Delta x=(-3,-1,2,2)$이므로 $x^\top\Delta x=-3-2+6+8=9$다. 값이 전부 같은 신호는 0점을 받는다.[^eq1-double]

이 9가 논문의 spectral 유도 전체의 씨앗이다. 여기서 $\hat A$까지 열다섯 걸음으로 자라는 과정은 [05 스펙트럼 다리](05_spectral_bridge.md)에 있고, 이 문서를 읽기 전에 읽는 것이 좋다.

[^eq1-double]: 식 (1)의 $\sum_{i,j}$는 순서쌍 이중합이라 무방향 간선 하나를 두 번 센다. 그래서 G4에서 좌변은 $2\times9=18$이고, $\Delta=D-A$로 계산한 우변 9와 상수 2만큼 다르다. 논문의 논지에는 영향이 없다.
```

## B2. 02 §4 — 축소 후 전체 텍스트 (30행)

`02:103`(`## 4. 왜 spectral graph convolution에서 시작하는가 { #spectral }`)부터 `02:223`(`그러므로 "GCN은 …` 문단 끝)까지 **전체**를 아래로 교체한다.

```markdown
## 4. 왜 spectral graph convolution에서 시작하는가 { #spectral }

논문은 식 (2)를 먼저 적고, 그 형태가 "그래프 위 국소 spectral 필터의 1차 근사로 동기를 줄 수 있다"고 말한 뒤 두 쪽에 걸쳐 식 (1)의 라플라시안에서 다시 출발한다. 그 두 쪽을 간선 하나의 뺄셈에서부터 열다섯 걸음으로 만든 것이 [05 스펙트럼 다리](05_spectral_bridge.md)다. 여기서는 **도착점만** 다시 받아 적는다.

| 논문 식 | 무엇인가 | 파라미터 | 05의 단계 |
|---|---|---:|---|
| (3) | $g_\theta\star x=Ug_\theta(\Lambda)U^\top x$ — 쪼개고, 모드마다 곱하고, 되돌린다 | $N$ | [S8](05_spectral_bridge.md#s8) |
| (4)(5) | Chebyshev 다항식으로 근사해 $U$를 지운다 | $K+1$ | [S9](05_spectral_bridge.md#s9)·[S11](05_spectral_bridge.md#s11) |
| (6) | $K=1$로 자른다 | $2$ | [S12](05_spectral_bridge.md#s12) |
| (7) | 두 파라미터를 하나로 묶는다 — $\theta(I_N+D^{-1/2}AD^{-1/2})x$ | $1$ | [S13](05_spectral_bridge.md#s13) |
| (8) | 연산자를 $\hat A$로 치환한다 | $C\times F$ | [S14](05_spectral_bridge.md#s14) |

[원문] 논문은 식 (7)의 연산자가 $[0,2]$ 범위의 고유값을 가지므로 깊은 모델에서 반복 적용하면 수치 불안정과 gradient 폭발/소실로 이어질 수 있다고 적고, **renormalization trick**이라는 이름으로 $I_N+D^{-1/2}AD^{-1/2}\to\tilde D^{-1/2}\tilde A\tilde D^{-1/2}$ 치환을 도입한다. 이 화살표에는 식 번호가 없다 — §2.2의 번호 없는 본문이다. [Kipf & Welling 2017, §2.2]

$$
Z=\tilde D^{-1/2}\tilde A\tilde D^{-1/2}X\Theta,
\qquad
\Theta\in\mathbb R^{C\times F}.
\tag{8}
$$

[Kipf & Welling 2017, Eq. (8)]

$\Theta$는 식 (2)의 $W^{(l)}$과 같은 물건이고, 이 식이 식 (2)의 한 층으로 이어진다.

### 4.1 이 유도에는 선택 지점이 네 개 있다 { #spectral-choices }

식 (3)에서 식 (8)까지는 매끄러운 치환 사슬처럼 보이지만, 네 곳은 필연이 아니다.

| | 지점 | 성격 | 05의 자리 |
|---|---|---|---|
| (a) | $K=1$ | **선택** — 층당 1-hop으로 묶고 층을 쌓기로 한 것이다. Chebyshev $K=2,3$도 논문이 함께 실험했다([Table 3](#propagation-comparison)). | [S12](05_spectral_bridge.md#s12) |
| (b) | $\theta=\theta'_0=-\theta'_1$ | **선택** — 파라미터와 연산을 줄이고 과적합을 억제하려는 모델링 결정이다. | [S13](05_spectral_bridge.md#s13) |
| (c) | $\lambda_{max}\approx2$ | **근사** — 논문은 학습 중 파라미터가 이 척도 변화에 적응할 것으로 기대한다고 적는다. G4는 경로 그래프라 마침 정확히 2이고, 근사가 근사로 보이는 것은 G4○의 1.809에서다. | [S11](05_spectral_bridge.md#s11) |
| (d) | $I+D^{-1/2}AD^{-1/2}\to\hat A$ | **연산자 교체** — 동치 변형이 **아니다**. 두 연산자는 자기 연결 계수도 간선 계수도 다르고, $\hat A$의 고유벡터는 $L$의 고유벡터가 아니다. | [S14](05_spectral_bridge.md#s14)·[S15](05_spectral_bridge.md#s15) |

그러므로 "GCN은 spectral graph convolution과 동치다"는 문장은 성립하지 않는다. 정확한 문장은 "GCN의 전파식은 spectral 필터의 1차 근사에서 **동기를 얻어** 유도되었고, 마지막 단계에서 연산자가 교체되었으며, 그 교체는 실험으로 정당화되었다"이다. 그 실험이 [9.3](#propagation-comparison)이다.
```

**삭제 목록**(위 교체로 사라지는 것들과 그 행선지):

| 삭제 대상 | 원래 위치 | 행선지 |
|---|---|---|
| §4.0 "출발점은 새 대상이 아니다" 두 문단(그림 배치 규약 공지) | 02:105–109 | 축/격자 분리 규약은 05 S6의 "축 규약 하나를 못 박는다"로 이동 |
| `figSpectral` 슬롯과 7프레임 폴백 | 02:111–113 | 05의 아홉 그림으로 분해. `figSpectral` 자체는 제거 |
| §4.1 제목·$L=U\Lambda U^\top$ 식·식 (3) 디스플레이·[해설] "격자 푸리에 모드" | 02:115–132 | 05 S4~S8. "격자 푸리에 모드" [해설]은 **폐기**(미지어로 미지어를 설명한다) |
| §4.2 제목·식 (4)(5) 디스플레이·[원문] $K$-localized | 02:134–151 | 05 S9~S11. $O(N^2)$과 이식성은 05 S9의 불편 세 가지로 확장 |
| §4.3 제목·식 (6)(7) 디스플레이·[해설] "필연이 아니라 모델링 선택" | 02:153–173 | 05 S12~S13. [해설]은 §4.1 표의 (b) 칸과 중복이므로 **폐기** |
| §4.4 제목·[원문] $[0,2]$·치환 화살표·식 (8) | 02:175–198 | 축소본에 [원문] 한 문단 + 식 (8)만 남긴다 |
| §4.4 [해설] "논문이 쓴 단어는 치환이다…" | 02:200 | 05 S14의 [해설]로 **문장 그대로** 이동. 단 "일반적으로 고유벡터도 다르다"는 구절은 05 S15의 반증으로 대체 |
| §4.4 [해설: 도출] 고유값 범위 두 줄 | 02:202–210 | 05 S15. "홀수 길이 닫힌 걸음" 논증은 검산 불가이므로 $\operatorname{spec}(\hat A)$ 네 숫자로 **대체** |
| §4.5 절 번호 | 02:212 | `### 4.1`로 번호만 바꾸고 앵커 `#spectral-choices`는 유지(§9.3·§13이 건다) |

## B3. 03:68 교체

`03:68`의 문장을 통째로 교체한다.

- **원문(삭제)**: "Kipf–Welling이 고른 것은 뒤쪽이다. 대가로 행 합이 1이 아니게 되지만, 행렬이 **대칭**으로 남아 무방향 그래프의 스펙트럼 해석이 유지되고 보내는 쪽 차수도 계수에 남는다."
- **교체**: "Kipf–Welling이 고른 것은 뒤쪽이다. 대가로 행 합이 1이 아니게 되지만, 행렬이 **대칭**으로 남아 보내는 쪽 차수가 계수에 남고 고유값·고유벡터가 전부 실수로 남는다. 두 번째 이유가 왜 논문에 중요한지 — 논문이 식 (2) 앞에 두 쪽을 적은 이유 — 는 [05 스펙트럼 다리](05_spectral_bridge.md#s6)가 간선 하나의 뺄셈에서부터 만든다."

## B4. index.md

**(i) 권장 읽기 순서** — `index.md:56`의 문장과 아래 표를 교체·추가한다.

- 56행 교체: "읽는 순서는 **01 → 03 → 05 → 02 → 04**입니다. 문서 번호는 파일의 이름이지 순서가 아닙니다. 03이 02보다 앞에 오는 이유는 하나입니다 — 논문의 식 (2)에 나오는 연산자 $\hat A$를 **만들어 보는** 곳이 03이고, 그것을 **쓰는** 곳이 02이기 때문입니다. 05가 그 사이에 오는 이유도 같습니다 — 논문이 식 (2) 앞에 적어 둔 두 쪽을, 논문의 순서가 아니라 개념이 자라는 순서로 먼저 걷습니다."
- 표에 3행으로 삽입하고 뒤의 두 행 번호를 4, 5로 민다.

```markdown
| 3 | [05 스펙트럼 다리](05_spectral_bridge.md) | 간선 하나의 뺄셈에서 $\hat A$까지 15단계, 고유벡터·기저·필터·Chebyshev, 파라미터 $4\to K{+}1\to2\to1\to C\times F$ | [고유벡터](05_spectral_bridge.md#s5) · [고유값 축](05_spectral_bridge.md#s6) · [분해](05_spectral_bridge.md#s7) · [필터 등가성](05_spectral_bridge.md#s8) · [K-hop](05_spectral_bridge.md#s10) · [Chebyshev](05_spectral_bridge.md#s11) · [폭발](05_spectral_bridge.md#s13) · [계수 교체](05_spectral_bridge.md#s14) · [반증](05_spectral_bridge.md#s15) |
```

또한 4행(02)의 "그 문서의 그림" 칸에서 `[스펙트럼](02_kipf2017_gcn_guide.md#spectral)`을 지운다(슬롯이 사라진다).

**(ii) 글 안의 기호 표** — `## 그림을 읽는 하나의 문법`의 기호 표는 **도형** 표이므로 건드리지 않고, `## 자료를 읽는 방법` 절 끝에 표 하나를 새로 붙인다.

```markdown
글 안에서 뜻이 고정된 기호는 넷입니다.

| 기호 | 읽는 법 | 고정된 뜻 |
|---|---|---|
| $\star$ | 스타 | 스펙트럼 합성곱 — 쪼개고, 모드마다 곱하고, 되돌리는 세 단계 전체의 이름표([05 S8](05_spectral_bridge.md#s8)) |
| $\operatorname{spec}(\cdot)$ | 스펙 | 그 행렬의 고유값 전부를 모은 집합. 한국어로는 **스펙트럼**으로만 씁니다([05 S6](05_spectral_bridge.md#s6)) |
| $\tilde A$, $\tilde D$ | 물결 | **자기 연결을 더했다**([03 §2.2](03_bridge_mlp_to_gcn.md#build-operator)) |
| $\tilde L$, $\tilde\Lambda$ | 물결 | **고유값을 $[-1,1]$로 눌러 넣었다** — 위와 뜻이 다릅니다([05 S11](05_spectral_bridge.md#s11)) |
```

## B5. mkdocs.yml

```yaml
# nav 순서가 곧 권장 읽기 순서다: 01 → 03 → 05 → 02 → 04.
# 03이 02보다 앞에 오는 이유는 Â를 만드는 곳이 03, 쓰는 곳이 02이기 때문이다.
# 05가 그 사이인 이유는, 02 §4가 도착 선언만 남기고 유도를 05로 옮겼기 때문이다.
# 앞의 번호는 파일 이름이지 순서가 아니다.
nav:
  - 시작하기: index.md
  - 01. GNN 입문: 01_gnn_gentle_guide.md
  - 03. MLP에서 GCN까지: 03_bridge_mlp_to_gcn.md
  - 05. 스펙트럼 다리: 05_spectral_bridge.md
  - 02. GCN 논문 해설: 02_kipf2017_gcn_guide.md
  - 04. 실습과 토론: 04_exercises.md
```

## B6. 02 §9.3 — Table 3의 Propagation model 열 복원

`02:448–458`의 표와 출처 줄을 교체한다.

```markdown
| 설명 | 전파 모델 | Citeseer | Cora | Pubmed |
|---|---|---:|---:|---:|
| Chebyshev 필터, 식 (5), $K=3$ | $\sum_{k=0}^{K}T_k(\tilde L)X\Theta_k$ | 69.8 | 79.5 | 74.4 |
| Chebyshev 필터, 식 (5), $K=2$ | 위와 같음 | 69.6 | 81.2 | 73.8 |
| 1차 모델, 식 (6) | $X\Theta_0+D^{-1/2}AD^{-1/2}X\Theta_1$ | 68.3 | 80.0 | 77.5 |
| 단일 파라미터, 식 (7) | $\left(I_N+D^{-1/2}AD^{-1/2}\right)X\Theta$ | 69.3 | 79.2 | 77.4 |
| renormalization trick, 식 (8) | $\tilde D^{-1/2}\tilde A\tilde D^{-1/2}X\Theta$ | **70.3** | **81.5** | **79.0** |
| 1차 항만 | $D^{-1/2}AD^{-1/2}X\Theta$ | 68.7 | 80.5 | 77.8 |
| 다층 퍼셉트론 | $X\Theta$ | 46.5 | 55.1 | 71.4 |

[Kipf & Welling 2017, Table 3. 숫자는 무작위 초기화 100회 반복의 평균 정확도다. 가운데 열은 원 표의 propagation model 열을 그대로 옮긴 것이다.]
```

그리고 그 아래 첫 [해설] 문단의 첫 문장을 교체한다.

- **원문(삭제)**: "이 표가 [4.5의 네 선택 지점](#spectral-choices)에 대응한다."
- **교체**: "가운데 열이 [4.1의 네 선택 지점](#spectral-choices)과 한 줄씩 맞물린다 — 각 행의 식을 [05](05_spectral_bridge.md)의 어느 걸음에서 만들었는지까지 따라갈 수 있다."

같은 문단 끝에 한 줄을 덧붙인다.

```markdown
[원문] 논문 스스로도 이 교체를 실험으로 정당화한다. "제안한 renormalize된 전파 모델(식 (8))이 소박한 1차 모델에 비해 효율(파라미터와 연산의 수가 더 적다)과 여러 데이터셋에서의 예측 성능을 **둘 다** 개선함을 보였다." [Kipf & Welling 2017, §7.1]
```

## B7. 02 §11·§13 역참조

**(i) §11** — `02:485`의 [해설] 첫 문장을 교체한다.

- **원문(삭제)**: "$\hat A$를 반복 적용한다는 것은 거듭제곱 $\hat A^k$다. [4.4의 고유값 계산](#spectral)에서 $\hat A$의 최대 고유값이 정확히 1이고 그 고유벡터가 $\sqrt{\tilde d}$였다."
- **교체**: "$\hat A$를 반복 적용한다는 것은 거듭제곱 $\hat A^k$다. [05 S15](05_spectral_bridge.md#s15)에서 $\hat A\sqrt{\tilde d}=\sqrt{\tilde d}$를 한 행으로 검산했고, 나머지 세 눈금이 $-0.2287,\,0.1667,\,0.7287$이었다."

**(ii) §11** — `02:481`의 [원문] 문단 끝에 한 줄을 덧붙인다(M-31).

```markdown
[해설] 이 실험의 설정은 [§9의 주 실험](#experiments)과 다르다. 여기서는 **전체 라벨**을 쓰고 5-fold 교차검증으로 400 epoch을 early stopping 없이 학습한다. §9는 클래스당 20개 라벨, 고정 분할, 200 epoch이다. 두 표의 숫자를 나란히 놓고 비교하면 안 된다. 그리고 residual 변형의 식은 $H^{(l+1)}=\sigma\!\left(\hat AH^{(l)}W^{(l)}\right)+H^{(l)}$이다. [Kipf & Welling 2017, Appendix B, Eq. (14)]
```

**(iii) §13** — "GCN은 spectral graph convolution과 동치다" 항목(`02:513–515`)의 본문을 교체한다.

- **원문(삭제)**: "아니다. 유도에는 [선택 지점이 네 개](#spectral-choices) 있고, 마지막 (d)는 근사도 정리도 아닌 **연산자 교체**다. $\hat A$와 $I+D^{-1/2}AD^{-1/2}$는 자기 연결 가중치가 다르고 고유값 범위도 다르다. 스펙트럼 유도는 식의 **출처**이지 식의 **증명**이 아니다."
- **교체**: "아니다. 유도에는 [선택 지점이 네 개](#spectral-choices) 있고, 마지막 (d)는 근사도 정리도 아닌 **연산자 교체**다. 다만 이유를 정확히 알아야 한다 — 동치를 깨는 것은 고유값 **범위**가 아니다. $I+D^{-1/2}AD^{-1/2}=2I-L$이므로 이 연산자는 $L$과 고유벡터가 완전히 같고 눈금만 뒤집힌, 어엿한 spectral 필터다. 깨지는 지점은 $\hat A$의 고유벡터 $\sqrt{\tilde d}$가 $L$의 고유벡터가 아니라는 것이고, [05 S15](05_spectral_bridge.md#s15)가 그것을 성분 두 개로 보여 준다. 스펙트럼 유도는 식의 **출처**이지 식의 **증명**이 아니다."

**(iv) §13** — "$\hat A$는 고유값이 작으니 깊게 쌓아도 안전하다" 항목 끝에 링크를 건다: "…한 몸이다. 숫자는 [05 S15](05_spectral_bridge.md#s15)의 $0.729^8=0.0795$다."

## B8. 04 — 문제 9~12와 해설

`04`의 문제 8 뒤, `---` 앞에 다음을 삽입한다. 그리고 `04:7`의 안내 문장에 "문제 9~12는 [05](05_spectral_bridge.md)의 열다섯 걸음을 되짚는다. 여기서만 자기 연결이 **없는** 차수 $d=(2,2,1,1)$을 쓴다(문제 12 후반 제외)."를 덧붙인다.

```markdown
## 문제 9. 뺄셈에서 라플라시안까지 { #p9 }

스칼라 특성 $x=(1,2,3,4)$를 쓴다(공통 그래프의 $X$ 첫 열이 아니라 [05](05_spectral_bridge.md)가 쓰는 신호다).

1. 세 간선에서 양 끝 값의 차를 각각 적고, 제곱해서 더한 값 $E(x)$를 구하라.
2. $\Delta=D-A$를 적고 $\Delta x$를 계산하라. 여기서 $D$는 자기 연결을 더하기 **전**의 차수다.
3. $x^\top\Delta x$가 1번의 답과 같음을 보이라.
4. 논문 식 (1)의 좌변 $\sum_{i,j}A_{ij}\lVert\cdot\rVert^2$을 그대로 계산하면 왜 18이 나오는가?

## 문제 10. 고유벡터 검산과 λ 눈금 { #p10 }

$L=I-D^{-1/2}AD^{-1/2}$를 쓴다.

1. $u_3=(0.4082,0.4082,-0.5774,-0.5774)$가 $\lambda=1.5$의 고유벡터임을 $L$의 **1행 하나만** 써서 확인하라.
2. 같은 방향을 정수로 쓴 $v=(1,1,-\sqrt2,-\sqrt2)$에 대해 $v^\top Lv$와 $\lVert v\rVert^2$를 구하고, 몫이 1.5임을 보이라.
3. $\lambda=0$의 고유벡터가 상수 신호가 **아닌** 이유를 한 문장으로 적어라.

## 문제 11. 분해와 필터 등가성 { #p11 }

1. $c_1=u_1^\top x$와 $c_3=u_3^\top x$를 계산하라.
2. $\sum_kc_k^2=30$과 $\sum_k\lambda_kc_k^2=12.4437$을 확인하라(둘째 것은 네 항 중 큰 두 개만 계산해도 근사로 확인된다).
3. 배수표 $g(\lambda)=1-\lambda$를 먹이고 되돌린 결과의 1행이 $Sx$의 1행과 같음을 한 줄로 보이라.
4. 배수표를 $(1,0,0,0)$으로 두면 결과가 어떤 벡터에 비례하는가?

## 문제 12. 폭발과 renormalization { #p12 }

1. $I+S$의 자기 고리 계수 넷과 간선 계수 셋을 모두 적어라.
2. $(I+S)x$를 계산하라. 그리고 $\operatorname{spec}(I+S)=\{0,0.5,1.5,2\}$를 $I+S=2I-L$에서 설명하라.
3. $\hat A$의 같은 일곱 계수를 적고, $v_1$의 자기/이웃 비가 어떻게 바뀌는지 두 수로 적어라.
4. $\hat A\sqrt{\tilde d}=\sqrt{\tilde d}$를 1행으로 검산하라.
5. $\sqrt{\tilde d}$를 $L$의 네 모드로 분해하면 0이 아닌 성분이 몇 개인가? 그 답이 왜 "$\hat A$는 식 (3)의 필터가 아니다"를 뜻하는가?
```

해설부에는 `## 해설 8` 뒤에 다음을 붙인다.

```markdown
## 해설 9

1. $1-2=-1$, $1-3=-2$, $2-4=-2$. 제곱해 더하면 $1+4+4=9$.
2. $d=(2,2,1,1)$이므로 $\Delta x=(2(1)-5,\ 2(2)-5,\ 1(3)-1,\ 1(4)-2)=(-3,-1,2,2)$.
3. $x^\top\Delta x=1(-3)+2(-1)+3(2)+4(2)=9$.
4. $\sum_{i,j}$는 순서쌍 이중합이라 무방향 간선 하나를 $(i,j)$와 $(j,i)$로 두 번 센다. $2\times9=18$이다. $f^\top\Delta f$ 쪽은 간선당 한 번이므로 9이고, 상수 2만큼 다르다.

## 해설 10

1. $L$의 1행은 $(1,\ -0.5,\ -0.7071,\ 0)$이다. $u_3$과 곱하면 $0.4082-0.5(0.4082)-0.7071(-0.5774)=0.4082-0.2041+0.4082=0.6123$이고, $1.5\times0.4082=0.6123$이다.
2. $v^\top Lv$는 간선 셋으로 쪼개진다. $d=(2,2,1,1)$이므로 간선 $(1,2)$는 $\left(\tfrac1{\sqrt2}-\tfrac1{\sqrt2}\right)^2=0$, 간선 $(1,3)$은 $\left(\tfrac1{\sqrt2}+\sqrt2\right)^2=(2.1213)^2=4.5$, 간선 $(2,4)$도 같은 이유로 $4.5$다. 합이 9이고 $\lVert v\rVert^2=1+1+2+2=6$이므로 몫은 $9/6=1.5$다. **여기서 재는 것은 차수로 한 번씩 나눈 판의 거칢이지 [05 S2](05_spectral_bridge.md#s2)의 날것 $E(x)$가 아니다.**
3. $L$이 차수로 한 번씩 나눈 판에서 거칢을 재기 때문이다. 그 판에서 거칢을 0으로 만드는 신호는 $\sqrt d=(\sqrt2,\sqrt2,1,1)$에 비례한다.

## 해설 11

1. $c_1=0.5774(1+2)+0.4082(3+4)=1.7321+2.8577=4.5898$. $c_3=0.4082(1+2)-0.5774(3+4)=1.2247-4.0415=-2.8167$.
2. $c=(4.5898,-0.9856,-2.8167,-0.1691)$이므로 제곱합은 $21.07+0.97+7.93+0.03=30=1+4+9+16$이다. $\lambda$를 곱하면 $0+0.4857+11.9007+0.0572=12.4436$이고, 이것이 $x^\top Lx$다.
3. $g(\lambda)=1-\lambda$면 $Ug(\Lambda)U^\top=I-L=S$다. $Sx$의 1행은 $0.5(2)+0.7071(3)=3.1213$이다.
4. $c_1u_1=4.5898\times(0.5774,0.5774,0.4082,0.4082)=(2.6504,2.6504,1.8738,1.8738)$이고 $\sqrt d=(1.4142,1.4142,1,1)$에 비례한다.

## 해설 12

1. 자기 고리는 넷 다 $1.0000$. 간선은 $(1,2)$가 $0.5000$, $(1,3)$과 $(2,4)$가 $0.7071$.
2. $(I+S)x=(4.121,\ 5.328,\ 3.707,\ 5.414)$. $I+S=2I-L$이므로 고유벡터는 $L$과 같고 고유값만 $\lambda\mapsto2-\lambda$다. $\{0,0.5,1.5,2\}$가 뒤집혀도 같은 집합이다. 가장 큰 눈금이 2이므로 8층이면 그 모드가 $2^8=256$배가 된다.
3. 자기 고리는 $v_1,v_2$에서 $1/3=0.3333$, $v_3,v_4$에서 $1/2=0.5000$. 간선은 $(1,2)$가 $0.3333$, $(1,3)$과 $(2,4)$가 $1/\sqrt6=0.4082$. $v_1$의 자기/이웃 비는 $1.0000/0.7071=1.414$에서 $0.3333/0.4082=0.8165$로 **뒤집힌다.**
4. $\sqrt{\tilde d}=(\sqrt3,\sqrt3,\sqrt2,\sqrt2)$이고 1행은 $\frac13\sqrt3+\frac13\sqrt3+\frac1{\sqrt6}\sqrt2=0.5774\times3=1.7321=\sqrt3$이다.
5. 둘이다($u_1$ 방향 3.155, $u_3$ 방향 $-0.219$). 식 (3)의 필터는 $L$의 기저를 그대로 두고 눈금만 바꾸므로, 그 꼴이려면 $\hat A$의 고유벡터가 $L$의 고유벡터여야 한다. 성분이 둘이면 아니다. 그래서 마지막 걸음은 근사가 아니라 교체다.
```

!!! note "문항 10-2에서 판을 섞지 않도록"
    "거칢"이라는 한 단어가 05 안에서 두 판을 가리킨다 — S2의 날것 $E(x)$와 S4의 차수 보정판 $x^\top Lx$다. S6의 정수 표와 문제 10-2는 **뒤쪽**이다. 그래서 문항과 표 머리 모두 $v^\top Lv$라고 기호로 못 박았고, 05 S6 본문에도 "여기서 재는 거칢은 S4의 것"이라는 한 줄을 두었다. 이 한 줄이 빠지면 독자가 $E(v)$를 날것으로 재어 11.657을 얻고 표와 어긋난다.

---

# 부록 C. 규칙 자체 점검표

findings §6의 열 규칙 각각에 대해 이 대본이 어디서 어떻게 지키는지.

| # | 규칙 | 이 대본에서 |
|---|---|---|
| 1 | 한 단계에 새 용어 0~1개, 용어는 계산 뒤에 | S1·S2 앞부분·S12·S13은 새 용어 0개. 나머지는 각 1개이고, 모든 단계가 "먼저 계산 → 이 물건에 이름을 붙이자: ○○"의 순서다. S4의 $S$는 "새 개념이 아니라 줄임 표기"라고 명시했고, S6의 '모드'와 '스펙트럼'은 같은 대상의 낱개와 집합이라 한 개념으로 센다. |
| 2 | 모든 수치는 G4에서 나오고 registry가 계산 | 본문의 모든 숫자를 재계산으로 확인했다(부록 A 머리에 정정 한 건). 부록 A0이 필요한 커널 9개와 `Ltilde`·`lmax`·`G4circ`를 먼저 넣도록 못 박았고, 계산 불가능한 프레임은 제안하지 않았다. |
| 3 | 고유벡터 부호 규약은 "첫 비영 성분 양수" 하나 | S5에 규약 문단을 두고 "반대 규약을 쓰면 S7의 분해 계수 부호가 통째로 뒤집힌다"고 적었다. S7의 $c$, S8의 새 계수, S15의 $-0.219$가 모두 이 규약의 값이다. 부록 A0이 `jacobiEig` 변경과 회귀 검증을 지시한다. |
| 4 | 차수 두 종을 매 단계 명시, 물결 두 뜻에 경고 | 서두 규약 문단에서 구간을 못 박고($d$는 S4–S13, $\tilde d$는 S14–), S4에 굵은 글씨로 한 번, S14의 계수 표에 차수를 캡션으로 한 번 더 적는다. 물결 경고는 S11(고유값 재척도)과 S14(자기 연결) 두 자리에 `!!! warning`으로 있고 서로를 가리킨다. |
| 5 | `[원문]`은 논문이 실제로 적은 문장에만, 페이지·식 번호 단위로 | [원문]은 일곱 곳이고 전부 §2.1/§2.2/§1/§7.1의 실제 문장을 옮긴 것이다. 치환 화살표는 S14에서 "§2.2의 번호 없는 본문 한 줄"이라고 명시했고, 그 사실을 (d) 논증의 근거로 쓴다. |
| 6 | `[해설]`은 G4 숫자 동반, `[해설: 도출]`은 한 행 검산 가능할 때만 | [해설: 도출]은 여섯 곳이고 전부 한 행 산술이다 — S3의 1행, S4의 간선 셋, S5의 3행, S7의 두 합계, S10의 $(S^3)_{34}$ 한 경로, S11의 $(S^2)_{11}$, S13의 $2^8$, S15의 $\hat A$ 1행과 분해 두 성분. "홀수 길이 닫힌 걸음" 논증은 S11의 패리티 설명으로 **수치와 함께** 내려 썼고, 02의 §4.4 [해설: 도출]에서는 부록 B2가 그것을 $\operatorname{spec}(\hat A)$ 네 숫자로 대체한다. |
| 7 | 그림 슬롯은 개념 직후 하나씩, 앞에 읽는 법 한 줄, 뒤에 폴백, 판정은 HTML 캡션 | 슬롯 아홉 개가 S5·S6·S7·S8·S10·S11·S13·S14·S15에 하나씩 있고 한 슬롯에 프레임 다섯 장을 넘지 않는다. 모든 슬롯 앞에 "아래 그림에서 …면 …이다" 한 줄이 있고, 폴백은 그 시점까지 본문이 정의한 말만 쓴다(S5 폴백에 '기저'나 '필터'가 없고, S11 폴백에 '치환'이 없다). 판정 문장은 부록 A의 HTML 캡션과 falsify에만 둔다. |
| 8 | 논문 식은 단계 끝에서 회수, 파라미터 개수 한 줄 | 각 단계의 마지막 블록이 **논문 식 회수**이고 식을 먼저 인쇄한 뒤 해설하지 않는다. 파라미터 사슬은 S8(4개=N) → S9($K{+}1$) → S12(2개) → S13(1개) → S14($C\times F$)에서 한 줄씩 회수되고, S10·S11·S15에서도 현재 개수를 한 줄로 반복한다. |
| 9 | 메타 명제는 두 번 이하 | 두 번이다. ① S14의 [해설] "정리한 것이 아니라 다른 연산자로 바꾼 것"(02:200 보존 문장), ② S15의 "동치가 아니다". 선택 지점 (a)(b)(c)는 "이 자리가 02 §4.5 표의 (a)다"처럼 **위치만** 가리키고 논평하지 않는다. "필연이 아니다"는 05 본문에 0회이고 02 §4.1 표에만 남는다. |
| 10 | G4로 볼 수 없는 것은 그 자리에서 밝힌다 | S11에 "G4로는 볼 수 없는 것이 있다" 문단이 있고 G4○의 $\lambda_{max}=1.809$·$2/\lambda_{max}=1.106$을 한 줄로 곁들인다. 확장은 여기 한 번뿐이고 계보를 늘리지 않는다(부록 A6은 G4○를 ③ 프레임의 축 캡션 한 줄로만 쓴다). $T_2(\tilde L)$의 패리티는 슬롯 **앞** 본문에서 미리 밝히고, 부록 A6의 ③ 캡션이 "그림이 틀린 것이 아니다"로 한 번 더 받는다. |
